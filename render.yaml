services:
  - type: static
    name: caja-el-titan
    rootDir: titan/frontend
    buildCommand: npm ci --legacy-peer-deps && npm run build
    publishDirectory: titan/frontend/build
    autoDeployOnPush: true
    branch: claude/claude-rc-txicjp
