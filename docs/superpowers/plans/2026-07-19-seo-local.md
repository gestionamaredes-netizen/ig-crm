# SEO Local — Plan de Implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir el módulo SEO Local dentro de IG CRM: barre una zona geográfica con Google Places, audita la ficha de Google de cada comercio contra sus competidores del rubro, y genera un informe imprimible con marca Iniciativa Global que se usa como material de venta.

**Architecture:** Módulo nuevo en `IG OS/web` bajo `app/(app)/seo-local/` y `lib/seo-local/`. El motor de auditoría es una función pura sin I/O (testeable sin red ni API key). El acceso a Google vive detrás de una interfaz `ProveedorSeoLocal` con dos implementaciones: Google Places y manual. La persistencia usa tres tablas nuevas en la Supabase existente.

**Tech Stack:** Next.js 16 (App Router), React 19, TypeScript strict, Drizzle (solo definición de esquema), Supabase JS (lecturas y escrituras), Vitest (nuevo), lucide-react, Google Places API (New).

## Global Constraints

- Directorio de trabajo: `C:\Users\Gesti\INICIATIVA GLOBAL\IG OS\web`. Todas las rutas del plan son relativas a ahí salvo indicación contraria.
- El repositorio git es `IG OS/` (un nivel arriba de `web/`). Los `git add` usan rutas con prefijo `web/`.
- **No existe carpeta `src/`.** El código vive en `app/`, `lib/`, `components/` en la raíz de `web/`.
- Alias de imports: `@/*` mapea a `./*` desde la raíz de `web/`. Usar `@/lib/...`, nunca rutas relativas largas.
- TypeScript en modo `strict`. Nada de `any` implícito.
- **Las lecturas y escrituras de datos usan el cliente de Supabase** (`createClient` de `@/lib/supabase/server`), siguiendo `lib/data.ts`. Drizzle se usa solo para definir el esquema en `lib/db/schema.ts`. No introducir consultas Drizzle en tiempo de ejecución.
- Estilos: JSX con `style={{...}}` en línea y variables CSS existentes: `var(--card)`, `var(--border)`, `var(--muted)`, `var(--grad)`. No agregar librerías de UI.
- Iconos: `lucide-react`, ya instalado.
- Todo el texto visible al usuario va en **español rioplatense**.
- Paleta de marca: fondo `#0B0B0D`, cards `#17181C`, gradiente `#5E5CE6 → #B14BFF → #FF6B6B → #FF9966`.
- Puntaje de auditoría: bloque ficha 40, reputación 30, competencia 30. Penalizaciones manuales: ficha no reclamada −10, no responde reseñas −5. **Piso en 0**, el total nunca es negativo.
- Zona inicial "Gorriti, Francisco Álvarez": punto A `-34.60208108, -58.86292683`, punto B `-34.60989714, -58.86890736`, radio 350 m.
- La API key se lee de `process.env.GOOGLE_PLACES_API_KEY`, **solo en código de servidor**. Nunca se expone al navegador ni se escribe en un archivo versionado.

## Desvíos respecto del spec

Dos puntos donde este plan se aparta del documento de diseño, con su razón:

**1. No se construye una clase `ProveedorManual`.** El spec la pedía como respaldo para operar sin API key. En la práctica, ese respaldo lo da la Tarea 8: cuando no hay clave o Google falla, las acciones auditan con los datos ya persistidos en `seo_negocios`. Una segunda implementación de la interfaz haría exactamente lo mismo con más código. La interfaz `ProveedorSeoLocal` queda igual, así que agregar DataForSEO más adelante sigue siendo escribir una implementación nueva y nada más.

**2. Los negocios "sin presencia en Google" no se pueden descubrir automáticamente.** El spec decía que un negocio sin ficha se marca como "sin presencia" y encabeza la lista de prospectos. Eso es imposible por construcción: la fuente de datos es Google, así que un negocio que no está en Google no aparece en ningún barrido. Detectarlos requiere caminar la calle o cruzar contra otro padrón, y ninguna de las dos cosas entra en esta fase. Lo que sí hace la Tarea 9 es ordenar por urgencia, dejando arriba a los que están en Google pero peor trabajados. Los negocios sin ficha quedan como una carga manual futura, no como una funcionalidad de esta fase.

---

### Task 1: Vitest y helper geográfico

Instala el framework de tests (no existe ninguno en el proyecto) y construye el cálculo de puntos de barrido, que es la base del módulo.

**Files:**
- Create: `vitest.config.ts`
- Create: `lib/seo-local/geo.ts`
- Test: `lib/seo-local/geo.test.ts`
- Modify: `package.json` (agregar script `test` y dependencias de desarrollo)

**Interfaces:**
- Consumes: nada (primera tarea)
- Produces:
  - `type Coordenada = { lat: number; lng: number }`
  - `function distanciaMetros(a: Coordenada, b: Coordenada): number`
  - `function puntosDeBarrido(a: Coordenada, b: Coordenada, radioMetros: number): Coordenada[]`

- [ ] **Step 1: Instalar Vitest**

```bash
cd "C:/Users/Gesti/INICIATIVA GLOBAL/IG OS/web"
npm install -D vitest@^3
```

- [ ] **Step 2: Crear la configuración de Vitest**

Crear `vitest.config.ts`:

```ts
import { defineConfig } from "vitest/config";
import path from "node:path";

export default defineConfig({
  test: {
    environment: "node",
    include: ["lib/**/*.test.ts"],
  },
  resolve: {
    alias: { "@": path.resolve(__dirname, ".") },
  },
});
```

- [ ] **Step 3: Agregar el script de test**

En `package.json`, dentro de `"scripts"`, agregar después de `"lint": "eslint",`:

```json
    "test": "vitest run",
    "test:watch": "vitest",
```

- [ ] **Step 4: Escribir el test que falla**

Crear `lib/seo-local/geo.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { distanciaMetros, puntosDeBarrido, type Coordenada } from "@/lib/seo-local/geo";

const GORRITI_A: Coordenada = { lat: -34.60208108, lng: -58.86292683 };
const GORRITI_B: Coordenada = { lat: -34.60989714, lng: -58.86890736 };

describe("distanciaMetros", () => {
  it("mide el tramo de Gorriti en ~1030 metros", () => {
    expect(distanciaMetros(GORRITI_A, GORRITI_B)).toBeCloseTo(1030, -2);
  });

  it("da 0 entre un punto y sí mismo", () => {
    expect(distanciaMetros(GORRITI_A, GORRITI_A)).toBe(0);
  });
});

describe("puntosDeBarrido", () => {
  it("genera 3 puntos para el tramo de Gorriti con radio 350", () => {
    const puntos = puntosDeBarrido(GORRITI_A, GORRITI_B, 350);
    expect(puntos).toHaveLength(3);
    expect(puntos[0].lat).toBeCloseTo(-34.603384, 5);
    expect(puntos[0].lng).toBeCloseTo(-58.863924, 5);
    expect(puntos[1].lat).toBeCloseTo(-34.605989, 5);
    expect(puntos[1].lng).toBeCloseTo(-58.865917, 5);
    expect(puntos[2].lat).toBeCloseTo(-34.608594, 5);
    expect(puntos[2].lng).toBeCloseTo(-58.867911, 5);
  });

  it("nunca genera menos de 2 puntos, aunque el tramo sea corto", () => {
    const cerca: Coordenada = { lat: -34.60208108, lng: -58.86292683 };
    const casiIgual: Coordenada = { lat: -34.60210000, lng: -58.86293000 };
    expect(puntosDeBarrido(cerca, casiIgual, 350)).toHaveLength(2);
  });

  it("cubre el tramo entero: ningún extremo queda fuera del radio de su círculo", () => {
    const puntos = puntosDeBarrido(GORRITI_A, GORRITI_B, 350);
    expect(distanciaMetros(GORRITI_A, puntos[0])).toBeLessThanOrEqual(350);
    expect(distanciaMetros(GORRITI_B, puntos[puntos.length - 1])).toBeLessThanOrEqual(350);
  });
});
```

- [ ] **Step 5: Correr el test para verificar que falla**

```bash
npm test
```

Esperado: FALLA con un error de resolución del módulo `@/lib/seo-local/geo` (el archivo todavía no existe).

- [ ] **Step 6: Implementar el helper geográfico**

Crear `lib/seo-local/geo.ts`:

```ts
export type Coordenada = { lat: number; lng: number };

const RADIO_TIERRA_M = 6_371_000;

const aRadianes = (grados: number) => (grados * Math.PI) / 180;

/** Distancia en metros entre dos coordenadas (fórmula de haversine). */
export function distanciaMetros(a: Coordenada, b: Coordenada): number {
  const dLat = aRadianes(b.lat - a.lat);
  const dLng = aRadianes(b.lng - a.lng);
  const h =
    Math.sin(dLat / 2) ** 2 +
    Math.cos(aRadianes(a.lat)) * Math.cos(aRadianes(b.lat)) * Math.sin(dLng / 2) ** 2;
  return 2 * RADIO_TIERRA_M * Math.asin(Math.sqrt(h));
}

/**
 * Reparte círculos de barrido a lo largo del tramo A→B.
 *
 * La cantidad no la manda solo la cobertura geométrica: dos círculos alcanzarían
 * para cubrir el tramo de Gorriti, pero Places devuelve como máximo 20 resultados
 * por consulta. En una calle comercial densa eso se llena y perdés negocios.
 * Por eso la densidad es de un círculo cada `radioMetros` de tramo, no cada 2.
 *
 * Los centros quedan en t = (i + 0.5) / n, o sea repartidos parejo con medio
 * paso de margen en cada punta.
 */
export function puntosDeBarrido(a: Coordenada, b: Coordenada, radioMetros: number): Coordenada[] {
  if (radioMetros <= 0) throw new Error("El radio de barrido debe ser mayor a 0");

  const largo = distanciaMetros(a, b);
  const cantidad = Math.max(2, Math.ceil(largo / radioMetros));

  return Array.from({ length: cantidad }, (_, i) => {
    const t = (i + 0.5) / cantidad;
    return {
      lat: a.lat + (b.lat - a.lat) * t,
      lng: a.lng + (b.lng - a.lng) * t,
    };
  });
}
```

- [ ] **Step 7: Correr el test para verificar que pasa**

```bash
npm test
```

Esperado: PASA, 5 tests en `lib/seo-local/geo.test.ts`.

- [ ] **Step 8: Commit**

```bash
cd "C:/Users/Gesti/INICIATIVA GLOBAL/IG OS"
git add web/package.json web/package-lock.json web/vitest.config.ts web/lib/seo-local/geo.ts web/lib/seo-local/geo.test.ts
git commit -m "SEO Local: Vitest y cálculo de puntos de barrido"
```

---

### Task 2: Tipos de dominio y auditoría de la ficha (40 puntos)

Define el vocabulario del módulo y la primera de las tres reglas de puntaje.

**Files:**
- Create: `lib/seo-local/tipos.ts`
- Create: `lib/seo-local/reglas-ficha.ts`
- Test: `lib/seo-local/reglas-ficha.test.ts`

**Interfaces:**
- Consumes: `Coordenada` de `@/lib/seo-local/geo`
- Produces:
  - `type FichaNegocio`, `type RevisionManual`, `type Hallazgo`, `type Gravedad`, `type ContextoCompetencia`, `type Auditoria` (en `tipos.ts`)
  - `const PUNTOS_FICHA = 40`
  - `function auditarFicha(ficha: FichaNegocio): Hallazgo[]`

- [ ] **Step 1: Crear los tipos de dominio**

Crear `lib/seo-local/tipos.ts`:

```ts
import type { Coordenada } from "@/lib/seo-local/geo";

/** Ficha de Google de un negocio, ya normalizada (no es el formato crudo de Places). */
export type FichaNegocio = {
  idExterno: string;
  nombre: string;
  rubro: string | null;
  direccion: string;
  ubicacion: Coordenada;
  telefono: string | null;
  sitioWeb: string | null;
  /** Cantidad de días de la semana con horario cargado, 0 a 7. */
  diasConHorario: number;
  /** Promedio de 1 a 5. null = todavía no tiene reseñas. */
  puntaje: number | null;
  cantidadResenas: number;
  /** Fecha ISO de la reseña más reciente. null = no hay reseñas. */
  fechaResenaMasReciente: string | null;
  cantidadFotos: number;
  estadoOperativo: "abierto" | "cerrado_temporal" | "cerrado_definitivo" | "desconocido";
};

/** Las dos cosas que Places no informa y hay que mirar a ojo. null = sin verificar. */
export type RevisionManual = {
  fichaReclamada: boolean | null;
  respondeResenas: boolean | null;
};

export type Gravedad = "alta" | "media" | "baja";

export type BloqueAuditoria = "ficha" | "reputacion" | "competencia" | "manual";

export type Hallazgo = {
  codigo: string;
  bloque: BloqueAuditoria;
  gravedad: Gravedad;
  puntosPerdidos: number;
  titulo: string;
  accion: string;
};

/**
 * Posición del negocio cuando alguien busca "rubro + localidad".
 * Número = puesto (base 1). "no_aparece" = se buscó y no está. "no_evaluado" = no se buscó.
 */
export type PosicionBusqueda = number | "no_aparece" | "no_evaluado";

export type ContextoCompetencia = {
  /** Fichas del mismo rubro en la misma zona, sin incluir al negocio auditado. */
  competidores: FichaNegocio[];
  posicionEnBusqueda: PosicionBusqueda;
};

export type Auditoria = {
  puntajeTotal: number;
  puntajeFicha: number;
  puntajeReputacion: number;
  puntajeCompetencia: number;
  penalizacionManual: number;
  hallazgos: Hallazgo[];
  /** Códigos de reglas que no se pudieron evaluar por falta de datos. */
  noEvaluados: string[];
  /** Puesto por cantidad de reseñas entre los del rubro (base 1). null = sin competidores. */
  puestoPorResenas: number | null;
  totalEnRubro: number;
  /** Cuántas reseñas faltan para pasar al de arriba. null = ya es primero o no hay datos. */
  resenasParaSubirUnPuesto: number | null;
};
```

- [ ] **Step 2: Escribir el test que falla**

Crear `lib/seo-local/reglas-ficha.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { auditarFicha, PUNTOS_FICHA } from "@/lib/seo-local/reglas-ficha";
import type { FichaNegocio } from "@/lib/seo-local/tipos";

/** Ficha impecable: no debería disparar ningún hallazgo del bloque. */
const fichaPerfecta: FichaNegocio = {
  idExterno: "places/perfecta",
  nombre: "Panadería Modelo",
  rubro: "bakery",
  direccion: "Gorriti 1200, Francisco Álvarez",
  ubicacion: { lat: -34.605, lng: -58.865 },
  telefono: "+54 237 400-0000",
  sitioWeb: "https://panaderiamodelo.com.ar",
  diasConHorario: 7,
  puntaje: 4.7,
  cantidadResenas: 120,
  fechaResenaMasReciente: "2026-07-01T00:00:00.000Z",
  cantidadFotos: 12,
  estadoOperativo: "abierto",
};

const con = (cambios: Partial<FichaNegocio>): FichaNegocio => ({ ...fichaPerfecta, ...cambios });

describe("auditarFicha", () => {
  it("no encuentra nada en una ficha completa", () => {
    expect(auditarFicha(fichaPerfecta)).toEqual([]);
  });

  it("detecta la falta de teléfono y descuenta 8", () => {
    const [h] = auditarFicha(con({ telefono: null }));
    expect(h.codigo).toBe("ficha_sin_telefono");
    expect(h.puntosPerdidos).toBe(8);
    expect(h.bloque).toBe("ficha");
  });

  it("trata el teléfono vacío igual que el ausente", () => {
    expect(auditarFicha(con({ telefono: "   " }))[0].codigo).toBe("ficha_sin_telefono");
  });

  it("detecta la falta de sitio web y descuenta 8", () => {
    const [h] = auditarFicha(con({ sitioWeb: null }));
    expect(h.codigo).toBe("ficha_sin_web");
    expect(h.puntosPerdidos).toBe(8);
  });

  it("detecta horarios incompletos y descuenta 8", () => {
    const [h] = auditarFicha(con({ diasConHorario: 5 }));
    expect(h.codigo).toBe("ficha_horarios_incompletos");
    expect(h.puntosPerdidos).toBe(8);
  });

  it("detecta la falta de rubro y descuenta 6", () => {
    const [h] = auditarFicha(con({ rubro: null }));
    expect(h.codigo).toBe("ficha_sin_rubro");
    expect(h.puntosPerdidos).toBe(6);
  });

  it("detecta menos de 5 fotos y descuenta 6", () => {
    const [h] = auditarFicha(con({ cantidadFotos: 4 }));
    expect(h.codigo).toBe("ficha_pocas_fotos");
    expect(h.puntosPerdidos).toBe(6);
  });

  it("acepta exactamente 5 fotos", () => {
    expect(auditarFicha(con({ cantidadFotos: 5 }))).toEqual([]);
  });

  it("detecta el negocio marcado como cerrado y lo marca grave", () => {
    const [h] = auditarFicha(con({ estadoOperativo: "cerrado_definitivo" }));
    expect(h.codigo).toBe("ficha_cerrado");
    expect(h.puntosPerdidos).toBe(4);
    expect(h.gravedad).toBe("alta");
  });

  it("suma exactamente 40 puntos perdidos en la ficha más rota posible", () => {
    const hallazgos = auditarFicha({
      ...fichaPerfecta,
      telefono: null,
      sitioWeb: null,
      diasConHorario: 0,
      rubro: null,
      cantidadFotos: 0,
      estadoOperativo: "cerrado_definitivo",
    });
    const total = hallazgos.reduce((s, h) => s + h.puntosPerdidos, 0);
    expect(total).toBe(PUNTOS_FICHA);
  });
});
```

- [ ] **Step 3: Correr el test para verificar que falla**

```bash
npm test lib/seo-local/reglas-ficha.test.ts
```

Esperado: FALLA por no poder resolver `@/lib/seo-local/reglas-ficha`.

- [ ] **Step 4: Implementar las reglas de la ficha**

Crear `lib/seo-local/reglas-ficha.ts`:

```ts
import type { FichaNegocio, Hallazgo } from "@/lib/seo-local/tipos";

/** Total de puntos que reparte el bloque de la ficha. */
export const PUNTOS_FICHA = 40;

const MINIMO_FOTOS = 5;

const vacio = (v: string | null): boolean => v === null || v.trim() === "";

/**
 * Bloque 1 — la ficha de Google (40 puntos).
 * Todo lo que evalúa se corrige en una tarde de trabajo, así que es lo primero
 * que se le muestra al prospecto: son las victorias rápidas.
 */
export function auditarFicha(ficha: FichaNegocio): Hallazgo[] {
  const hallazgos: Hallazgo[] = [];

  if (vacio(ficha.telefono)) {
    hallazgos.push({
      codigo: "ficha_sin_telefono",
      bloque: "ficha",
      gravedad: "alta",
      puntosPerdidos: 8,
      titulo: "No tenés teléfono cargado en Google",
      accion: "Cargar el teléfono para que aparezca el botón de llamar en el celular.",
    });
  }

  if (vacio(ficha.sitioWeb)) {
    hallazgos.push({
      codigo: "ficha_sin_web",
      bloque: "ficha",
      gravedad: "media",
      puntosPerdidos: 8,
      titulo: "No tenés sitio web enlazado",
      accion: "Enlazar un sitio o una landing. Sin web, Google tiene menos con qué posicionarte.",
    });
  }

  if (ficha.diasConHorario < 7) {
    hallazgos.push({
      codigo: "ficha_horarios_incompletos",
      bloque: "ficha",
      gravedad: "alta",
      puntosPerdidos: 8,
      titulo: `Tenés horarios cargados en ${ficha.diasConHorario} de 7 días`,
      accion: "Completar los 7 días. Sin horario, Google no te muestra como \"abierto ahora\".",
    });
  }

  if (vacio(ficha.rubro)) {
    hallazgos.push({
      codigo: "ficha_sin_rubro",
      bloque: "ficha",
      gravedad: "alta",
      puntosPerdidos: 6,
      titulo: "No tenés categoría principal definida",
      accion: "Elegir la categoría principal. Es lo que decide en qué búsquedas aparecés.",
    });
  }

  if (ficha.cantidadFotos < MINIMO_FOTOS) {
    hallazgos.push({
      codigo: "ficha_pocas_fotos",
      bloque: "ficha",
      gravedad: "media",
      puntosPerdidos: 6,
      titulo: `Tenés ${ficha.cantidadFotos} foto${ficha.cantidadFotos === 1 ? "" : "s"}`,
      accion: `Subir al menos ${MINIMO_FOTOS} fotos: frente del local, interior y productos.`,
    });
  }

  if (ficha.estadoOperativo === "cerrado_temporal" || ficha.estadoOperativo === "cerrado_definitivo") {
    hallazgos.push({
      codigo: "ficha_cerrado",
      bloque: "ficha",
      gravedad: "alta",
      puntosPerdidos: 4,
      titulo: "Google te muestra como cerrado",
      accion: "Corregir el estado del negocio en la ficha. Estás perdiendo clientes que te creen cerrado.",
    });
  }

  return hallazgos;
}
```

- [ ] **Step 5: Correr el test para verificar que pasa**

```bash
npm test lib/seo-local/reglas-ficha.test.ts
```

Esperado: PASA, 10 tests.

- [ ] **Step 6: Commit**

```bash
cd "C:/Users/Gesti/INICIATIVA GLOBAL/IG OS"
git add web/lib/seo-local/tipos.ts web/lib/seo-local/reglas-ficha.ts web/lib/seo-local/reglas-ficha.test.ts
git commit -m "SEO Local: tipos de dominio y reglas de la ficha"
```

---

### Task 3: Auditoría de la reputación (30 puntos)

**Files:**
- Create: `lib/seo-local/reglas-reputacion.ts`
- Test: `lib/seo-local/reglas-reputacion.test.ts`

**Interfaces:**
- Consumes: `FichaNegocio`, `Hallazgo` de `@/lib/seo-local/tipos`
- Produces:
  - `const PUNTOS_REPUTACION = 30`
  - `function auditarReputacion(ficha: FichaNegocio, ahora: Date): { hallazgos: Hallazgo[]; noEvaluados: string[] }`

**Nota de diseño:** `ahora` se recibe como parámetro en vez de leer el reloj adentro. Sin eso la regla de "reseñas viejas" no se puede testear de forma determinista.

- [ ] **Step 1: Escribir el test que falla**

Crear `lib/seo-local/reglas-reputacion.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { auditarReputacion, PUNTOS_REPUTACION } from "@/lib/seo-local/reglas-reputacion";
import type { FichaNegocio } from "@/lib/seo-local/tipos";

const AHORA = new Date("2026-07-19T00:00:00.000Z");

const fichaSana: FichaNegocio = {
  idExterno: "places/sana",
  nombre: "Panadería Modelo",
  rubro: "bakery",
  direccion: "Gorriti 1200",
  ubicacion: { lat: -34.605, lng: -58.865 },
  telefono: "+54 237 400-0000",
  sitioWeb: "https://ejemplo.com.ar",
  diasConHorario: 7,
  puntaje: 4.6,
  cantidadResenas: 80,
  fechaResenaMasReciente: "2026-07-10T00:00:00.000Z",
  cantidadFotos: 10,
  estadoOperativo: "abierto",
};

const con = (cambios: Partial<FichaNegocio>): FichaNegocio => ({ ...fichaSana, ...cambios });

describe("auditarReputacion", () => {
  it("no encuentra nada en un negocio con buena reputación", () => {
    const { hallazgos, noEvaluados } = auditarReputacion(fichaSana, AHORA);
    expect(hallazgos).toEqual([]);
    expect(noEvaluados).toEqual([]);
  });

  it("detecta puntaje menor a 4.0 y descuenta 12", () => {
    const { hallazgos } = auditarReputacion(con({ puntaje: 3.9 }), AHORA);
    const h = hallazgos.find((x) => x.codigo === "rep_puntaje_bajo");
    expect(h?.puntosPerdidos).toBe(12);
  });

  it("acepta exactamente 4.0", () => {
    const { hallazgos } = auditarReputacion(con({ puntaje: 4.0 }), AHORA);
    expect(hallazgos.find((x) => x.codigo === "rep_puntaje_bajo")).toBeUndefined();
  });

  it("detecta menos de 10 reseñas y descuenta 12", () => {
    const { hallazgos } = auditarReputacion(con({ cantidadResenas: 9 }), AHORA);
    const h = hallazgos.find((x) => x.codigo === "rep_pocas_resenas");
    expect(h?.puntosPerdidos).toBe(12);
  });

  it("detecta reseñas de más de 6 meses y descuenta 6", () => {
    const { hallazgos } = auditarReputacion(
      con({ fechaResenaMasReciente: "2025-12-01T00:00:00.000Z" }),
      AHORA,
    );
    const h = hallazgos.find((x) => x.codigo === "rep_resenas_viejas");
    expect(h?.puntosPerdidos).toBe(6);
  });

  it("no marca como viejas las reseñas de hace 3 meses", () => {
    const { hallazgos } = auditarReputacion(
      con({ fechaResenaMasReciente: "2026-04-19T00:00:00.000Z" }),
      AHORA,
    );
    expect(hallazgos.find((x) => x.codigo === "rep_resenas_viejas")).toBeUndefined();
  });

  it("con cero reseñas descuenta todo el bloque y no evalúa puntaje ni antigüedad", () => {
    const { hallazgos, noEvaluados } = auditarReputacion(
      con({ cantidadResenas: 0, puntaje: null, fechaResenaMasReciente: null }),
      AHORA,
    );
    const total = hallazgos.reduce((s, h) => s + h.puntosPerdidos, 0);
    expect(total).toBe(PUNTOS_REPUTACION);
    expect(noEvaluados).toEqual([]);
    expect(hallazgos.find((x) => x.codigo === "rep_sin_resenas")).toBeDefined();
  });

  it("declara no evaluado el puntaje si falta el dato pero hay reseñas", () => {
    const { noEvaluados } = auditarReputacion(con({ puntaje: null }), AHORA);
    expect(noEvaluados).toContain("rep_puntaje_bajo");
  });

  it("suma exactamente 30 puntos en el peor caso con reseñas", () => {
    const { hallazgos } = auditarReputacion(
      con({ puntaje: 2.0, cantidadResenas: 3, fechaResenaMasReciente: "2024-01-01T00:00:00.000Z" }),
      AHORA,
    );
    const total = hallazgos.reduce((s, h) => s + h.puntosPerdidos, 0);
    expect(total).toBe(PUNTOS_REPUTACION);
  });
});
```

- [ ] **Step 2: Correr el test para verificar que falla**

```bash
npm test lib/seo-local/reglas-reputacion.test.ts
```

Esperado: FALLA por no poder resolver `@/lib/seo-local/reglas-reputacion`.

- [ ] **Step 3: Implementar las reglas de reputación**

Crear `lib/seo-local/reglas-reputacion.ts`:

```ts
import type { FichaNegocio, Hallazgo } from "@/lib/seo-local/tipos";

/** Total de puntos que reparte el bloque de reputación. */
export const PUNTOS_REPUTACION = 30;

const PUNTAJE_MINIMO = 4.0;
const RESENAS_MINIMAS = 10;
const MESES_PARA_VIEJA = 6;
const DIAS_PARA_VIEJA = MESES_PARA_VIEJA * 30;

/**
 * Bloque 2 — reputación (30 puntos).
 *
 * Caso especial: un negocio sin ninguna reseña pierde el bloque entero de una,
 * con un solo hallazgo. No tiene sentido decirle "tenés pocas reseñas" y además
 * "tu puntaje es bajo" cuando el problema es uno solo y evidente.
 *
 * `ahora` entra por parámetro para que la regla de antigüedad sea determinista.
 */
export function auditarReputacion(
  ficha: FichaNegocio,
  ahora: Date,
): { hallazgos: Hallazgo[]; noEvaluados: string[] } {
  const hallazgos: Hallazgo[] = [];
  const noEvaluados: string[] = [];

  if (ficha.cantidadResenas === 0) {
    hallazgos.push({
      codigo: "rep_sin_resenas",
      bloque: "reputacion",
      gravedad: "alta",
      puntosPerdidos: PUNTOS_REPUTACION,
      titulo: "No tenés ninguna reseña",
      accion: "Pedirle reseña a los clientes habituales. Es lo que más mueve el posicionamiento local.",
    });
    return { hallazgos, noEvaluados };
  }

  if (ficha.puntaje === null) {
    noEvaluados.push("rep_puntaje_bajo");
  } else if (ficha.puntaje < PUNTAJE_MINIMO) {
    hallazgos.push({
      codigo: "rep_puntaje_bajo",
      bloque: "reputacion",
      gravedad: "alta",
      puntosPerdidos: 12,
      titulo: `Tu puntaje es ${ficha.puntaje.toFixed(1)} de 5`,
      accion: `Trabajar las reseñas para superar ${PUNTAJE_MINIMO.toFixed(1)}. Debajo de eso, la gente elige al de al lado.`,
    });
  }

  if (ficha.cantidadResenas < RESENAS_MINIMAS) {
    hallazgos.push({
      codigo: "rep_pocas_resenas",
      bloque: "reputacion",
      gravedad: "alta",
      puntosPerdidos: 12,
      titulo: `Tenés ${ficha.cantidadResenas} reseña${ficha.cantidadResenas === 1 ? "" : "s"}`,
      accion: `Llegar a ${RESENAS_MINIMAS} como piso. Es el mínimo para que Google te tome en serio.`,
    });
  }

  if (ficha.fechaResenaMasReciente === null) {
    noEvaluados.push("rep_resenas_viejas");
  } else {
    const dias = (ahora.getTime() - new Date(ficha.fechaResenaMasReciente).getTime()) / 86_400_000;
    if (dias > DIAS_PARA_VIEJA) {
      hallazgos.push({
        codigo: "rep_resenas_viejas",
        bloque: "reputacion",
        gravedad: "media",
        puntosPerdidos: 6,
        titulo: `Tu última reseña es de hace ${Math.floor(dias / 30)} meses`,
        accion: "Conseguir reseñas nuevas seguido. Google lee el silencio como negocio apagado.",
      });
    }
  }

  return { hallazgos, noEvaluados };
}
```

- [ ] **Step 4: Correr el test para verificar que pasa**

```bash
npm test lib/seo-local/reglas-reputacion.test.ts
```

Esperado: PASA, 9 tests.

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/Gesti/INICIATIVA GLOBAL/IG OS"
git add web/lib/seo-local/reglas-reputacion.ts web/lib/seo-local/reglas-reputacion.test.ts
git commit -m "SEO Local: reglas de reputación"
```

---

### Task 4: Auditoría de la competencia (30 puntos)

El bloque que cierra la venta: dónde está parado el negocio frente a los vecinos de su mismo rubro.

**Files:**
- Create: `lib/seo-local/reglas-competencia.ts`
- Test: `lib/seo-local/reglas-competencia.test.ts`

**Interfaces:**
- Consumes: `FichaNegocio`, `Hallazgo`, `ContextoCompetencia` de `@/lib/seo-local/tipos`
- Produces:
  - `const PUNTOS_COMPETENCIA = 30`
  - `type ResultadoCompetencia = { hallazgos: Hallazgo[]; noEvaluados: string[]; puestoPorResenas: number | null; totalEnRubro: number; resenasParaSubirUnPuesto: number | null }`
  - `function auditarCompetencia(ficha: FichaNegocio, contexto: ContextoCompetencia): ResultadoCompetencia`

- [ ] **Step 1: Escribir el test que falla**

Crear `lib/seo-local/reglas-competencia.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { auditarCompetencia, PUNTOS_COMPETENCIA } from "@/lib/seo-local/reglas-competencia";
import type { FichaNegocio } from "@/lib/seo-local/tipos";

const base: FichaNegocio = {
  idExterno: "places/base",
  nombre: "Negocio",
  rubro: "bakery",
  direccion: "Gorriti 1200",
  ubicacion: { lat: -34.605, lng: -58.865 },
  telefono: "+54 237 400-0000",
  sitioWeb: "https://ejemplo.com.ar",
  diasConHorario: 7,
  puntaje: 4.5,
  cantidadResenas: 50,
  fechaResenaMasReciente: "2026-07-10T00:00:00.000Z",
  cantidadFotos: 10,
  estadoOperativo: "abierto",
};

const rival = (id: string, resenas: number, puntaje: number): FichaNegocio => ({
  ...base,
  idExterno: id,
  nombre: `Rival ${id}`,
  cantidadResenas: resenas,
  puntaje,
});

describe("auditarCompetencia", () => {
  it("no penaliza al líder del rubro", () => {
    const { hallazgos, puestoPorResenas } = auditarCompetencia(base, {
      competidores: [rival("a", 10, 3.0), rival("b", 20, 3.5)],
      posicionEnBusqueda: 1,
    });
    expect(puestoPorResenas).toBe(1);
    expect(hallazgos).toEqual([]);
  });

  it("calcula el puesto por reseñas contando a los competidores", () => {
    const { puestoPorResenas, totalEnRubro } = auditarCompetencia(base, {
      competidores: [rival("a", 100, 4.8), rival("b", 80, 4.7), rival("c", 10, 4.0)],
      posicionEnBusqueda: 1,
    });
    expect(puestoPorResenas).toBe(3);
    expect(totalEnRubro).toBe(4);
  });

  it("dice cuántas reseñas faltan para pasar al de arriba", () => {
    const { resenasParaSubirUnPuesto } = auditarCompetencia(base, {
      competidores: [rival("a", 64, 4.8)],
      posicionEnBusqueda: 1,
    });
    expect(resenasParaSubirUnPuesto).toBe(15);
  });

  it("devuelve null en reseñas para subir si ya es primero", () => {
    const { resenasParaSubirUnPuesto } = auditarCompetencia(base, {
      competidores: [rival("a", 10, 3.0)],
      posicionEnBusqueda: 1,
    });
    expect(resenasParaSubirUnPuesto).toBeNull();
  });

  it("penaliza con 12 estar en el tercio inferior por reseñas", () => {
    const { hallazgos } = auditarCompetencia(base, {
      competidores: [rival("a", 300, 4.9), rival("b", 200, 4.8), rival("c", 150, 4.7), rival("d", 120, 4.6), rival("e", 100, 4.5)],
      posicionEnBusqueda: 1,
    });
    const h = hallazgos.find((x) => x.codigo === "comp_puesto_resenas");
    expect(h?.puntosPerdidos).toBe(12);
  });

  it("penaliza con 8 estar en el tercio inferior por puntaje", () => {
    const { hallazgos } = auditarCompetencia({ ...base, puntaje: 3.1 }, {
      competidores: [rival("a", 10, 4.9), rival("b", 10, 4.8), rival("c", 10, 4.7)],
      posicionEnBusqueda: 1,
    });
    const h = hallazgos.find((x) => x.codigo === "comp_puesto_puntaje");
    expect(h?.puntosPerdidos).toBe(8);
  });

  it("penaliza con 10 no aparecer en la búsqueda", () => {
    const { hallazgos } = auditarCompetencia(base, {
      competidores: [rival("a", 10, 3.0)],
      posicionEnBusqueda: "no_aparece",
    });
    const h = hallazgos.find((x) => x.codigo === "comp_fuera_de_busqueda");
    expect(h?.puntosPerdidos).toBe(10);
  });

  it("penaliza con 10 quedar más abajo del puesto 5", () => {
    const { hallazgos } = auditarCompetencia(base, {
      competidores: [rival("a", 10, 3.0)],
      posicionEnBusqueda: 6,
    });
    expect(hallazgos.find((x) => x.codigo === "comp_fuera_de_busqueda")).toBeDefined();
  });

  it("acepta el puesto 5 en la búsqueda", () => {
    const { hallazgos } = auditarCompetencia(base, {
      competidores: [rival("a", 10, 3.0)],
      posicionEnBusqueda: 5,
    });
    expect(hallazgos.find((x) => x.codigo === "comp_fuera_de_busqueda")).toBeUndefined();
  });

  it("declara no evaluada la búsqueda si no se corrió", () => {
    const { noEvaluados, hallazgos } = auditarCompetencia(base, {
      competidores: [rival("a", 10, 3.0)],
      posicionEnBusqueda: "no_evaluado",
    });
    expect(noEvaluados).toContain("comp_fuera_de_busqueda");
    expect(hallazgos.find((x) => x.codigo === "comp_fuera_de_busqueda")).toBeUndefined();
  });

  it("sin competidores no evalúa los puestos y no inventa penalización", () => {
    const { hallazgos, noEvaluados, puestoPorResenas, totalEnRubro } = auditarCompetencia(base, {
      competidores: [],
      posicionEnBusqueda: 1,
    });
    expect(hallazgos).toEqual([]);
    expect(noEvaluados).toContain("comp_puesto_resenas");
    expect(noEvaluados).toContain("comp_puesto_puntaje");
    expect(puestoPorResenas).toBeNull();
    expect(totalEnRubro).toBe(1);
  });

  it("suma exactamente 30 puntos en el peor caso", () => {
    const { hallazgos } = auditarCompetencia({ ...base, cantidadResenas: 1, puntaje: 2.0 }, {
      competidores: [rival("a", 300, 4.9), rival("b", 200, 4.8), rival("c", 150, 4.7)],
      posicionEnBusqueda: "no_aparece",
    });
    const total = hallazgos.reduce((s, h) => s + h.puntosPerdidos, 0);
    expect(total).toBe(PUNTOS_COMPETENCIA);
  });
});
```

- [ ] **Step 2: Correr el test para verificar que falla**

```bash
npm test lib/seo-local/reglas-competencia.test.ts
```

Esperado: FALLA por no poder resolver `@/lib/seo-local/reglas-competencia`.

- [ ] **Step 3: Implementar las reglas de competencia**

Crear `lib/seo-local/reglas-competencia.ts`:

```ts
import type { ContextoCompetencia, FichaNegocio, Hallazgo } from "@/lib/seo-local/tipos";

/** Total de puntos que reparte el bloque de competencia. */
export const PUNTOS_COMPETENCIA = 30;

const TOPE_BUSQUEDA = 5;

export type ResultadoCompetencia = {
  hallazgos: Hallazgo[];
  noEvaluados: string[];
  puestoPorResenas: number | null;
  totalEnRubro: number;
  resenasParaSubirUnPuesto: number | null;
};

/** Puesto (base 1) del valor propio dentro del conjunto, de mayor a menor. */
function puesto(propio: number, ajenos: number[]): number {
  return ajenos.filter((v) => v > propio).length + 1;
}

/** ¿Está en el tercio de abajo? Con 6 de 6, sí; con 2 de 6, no. */
function enTercioInferior(puestoActual: number, total: number): boolean {
  return puestoActual > (total * 2) / 3;
}

/**
 * Bloque 3 — competencia (30 puntos).
 * Compara contra los negocios del mismo rubro en la misma zona. Es el bloque
 * que convierte el diagnóstico en argumento de venta: no le decís "estás mal",
 * le decís "estás 6º de 9 y te faltan 14 reseñas para subir".
 */
export function auditarCompetencia(
  ficha: FichaNegocio,
  contexto: ContextoCompetencia,
): ResultadoCompetencia {
  const hallazgos: Hallazgo[] = [];
  const noEvaluados: string[] = [];
  const { competidores, posicionEnBusqueda } = contexto;

  const totalEnRubro = competidores.length + 1;
  let puestoPorResenas: number | null = null;
  let resenasParaSubirUnPuesto: number | null = null;

  if (competidores.length === 0) {
    // Sin nadie con quien comparar no se inventa un veredicto.
    noEvaluados.push("comp_puesto_resenas", "comp_puesto_puntaje");
  } else {
    puestoPorResenas = puesto(ficha.cantidadResenas, competidores.map((c) => c.cantidadResenas));

    // Reseñas que faltan para superar al inmediato superior.
    const porEncima = competidores
      .map((c) => c.cantidadResenas)
      .filter((v) => v > ficha.cantidadResenas)
      .sort((a, b) => a - b);
    if (porEncima.length > 0) {
      resenasParaSubirUnPuesto = porEncima[0] - ficha.cantidadResenas + 1;
    }

    if (enTercioInferior(puestoPorResenas, totalEnRubro)) {
      hallazgos.push({
        codigo: "comp_puesto_resenas",
        bloque: "competencia",
        gravedad: "alta",
        puntosPerdidos: 12,
        titulo: `Estás ${puestoPorResenas}º de ${totalEnRubro} en cantidad de reseñas`,
        accion:
          resenasParaSubirUnPuesto !== null
            ? `Con ${resenasParaSubirUnPuesto} reseñas más subís un puesto.`
            : "Sumar reseñas para escalar en el rubro.",
      });
    }

    // El puntaje puede faltar; en ese caso no se compara.
    if (ficha.puntaje === null) {
      noEvaluados.push("comp_puesto_puntaje");
    } else {
      const puntajesAjenos = competidores
        .map((c) => c.puntaje)
        .filter((p): p is number => p !== null);
      if (puntajesAjenos.length === 0) {
        noEvaluados.push("comp_puesto_puntaje");
      } else {
        const puestoPuntaje = puesto(ficha.puntaje, puntajesAjenos);
        if (enTercioInferior(puestoPuntaje, puntajesAjenos.length + 1)) {
          hallazgos.push({
            codigo: "comp_puesto_puntaje",
            bloque: "competencia",
            gravedad: "media",
            puntosPerdidos: 8,
            titulo: `Estás ${puestoPuntaje}º de ${puntajesAjenos.length + 1} en puntaje`,
            accion: "Mejorar la experiencia y pedir reseñas a los clientes conformes.",
          });
        }
      }
    }
  }

  if (posicionEnBusqueda === "no_evaluado") {
    noEvaluados.push("comp_fuera_de_busqueda");
  } else if (posicionEnBusqueda === "no_aparece" || posicionEnBusqueda > TOPE_BUSQUEDA) {
    hallazgos.push({
      codigo: "comp_fuera_de_busqueda",
      bloque: "competencia",
      gravedad: "alta",
      puntosPerdidos: 10,
      titulo:
        posicionEnBusqueda === "no_aparece"
          ? "No aparecés cuando buscan tu rubro en la zona"
          : `Aparecés ${posicionEnBusqueda}º cuando buscan tu rubro en la zona`,
      accion: `Entrar a los primeros ${TOPE_BUSQUEDA} resultados. Abajo de ahí casi nadie mira.`,
    });
  }

  return { hallazgos, noEvaluados, puestoPorResenas, totalEnRubro, resenasParaSubirUnPuesto };
}
```

- [ ] **Step 4: Correr el test para verificar que pasa**

```bash
npm test lib/seo-local/reglas-competencia.test.ts
```

Esperado: PASA, 12 tests.

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/Gesti/INICIATIVA GLOBAL/IG OS"
git add web/lib/seo-local/reglas-competencia.ts web/lib/seo-local/reglas-competencia.test.ts
git commit -m "SEO Local: reglas de competencia"
```

---

### Task 5: Motor de auditoría completo

Une los tres bloques, aplica las penalizaciones manuales y produce el puntaje final con piso en 0.

**Files:**
- Create: `lib/seo-local/auditoria.ts`
- Test: `lib/seo-local/auditoria.test.ts`

**Interfaces:**
- Consumes: `auditarFicha`/`PUNTOS_FICHA`, `auditarReputacion`/`PUNTOS_REPUTACION`, `auditarCompetencia`/`PUNTOS_COMPETENCIA`, y los tipos de `@/lib/seo-local/tipos`
- Produces:
  - `function auditar(ficha: FichaNegocio, contexto: ContextoCompetencia, manual: RevisionManual, ahora: Date): Auditoria`
  - `function resumenDeVenta(auditoria: Auditoria): string`

- [ ] **Step 1: Escribir el test que falla**

Crear `lib/seo-local/auditoria.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { auditar, resumenDeVenta } from "@/lib/seo-local/auditoria";
import type { FichaNegocio, RevisionManual } from "@/lib/seo-local/tipos";

const AHORA = new Date("2026-07-19T00:00:00.000Z");

const perfecta: FichaNegocio = {
  idExterno: "places/perfecta",
  nombre: "Panadería Modelo",
  rubro: "bakery",
  direccion: "Gorriti 1200",
  ubicacion: { lat: -34.605, lng: -58.865 },
  telefono: "+54 237 400-0000",
  sitioWeb: "https://ejemplo.com.ar",
  diasConHorario: 7,
  puntaje: 4.8,
  cantidadResenas: 200,
  fechaResenaMasReciente: "2026-07-15T00:00:00.000Z",
  cantidadFotos: 20,
  estadoOperativo: "abierto",
};

const manualOk: RevisionManual = { fichaReclamada: true, respondeResenas: true };
const sinVerificar: RevisionManual = { fichaReclamada: null, respondeResenas: null };

describe("auditar", () => {
  it("da 100 a un negocio impecable y líder", () => {
    const a = auditar(perfecta, { competidores: [], posicionEnBusqueda: 1 }, manualOk, AHORA);
    expect(a.puntajeTotal).toBe(100);
    expect(a.puntajeFicha).toBe(40);
    expect(a.puntajeReputacion).toBe(30);
    expect(a.puntajeCompetencia).toBe(30);
    expect(a.penalizacionManual).toBe(0);
  });

  it("descuenta 10 si la ficha no está reclamada", () => {
    const a = auditar(
      perfecta,
      { competidores: [], posicionEnBusqueda: 1 },
      { fichaReclamada: false, respondeResenas: true },
      AHORA,
    );
    expect(a.penalizacionManual).toBe(10);
    expect(a.puntajeTotal).toBe(90);
  });

  it("descuenta 5 si no responde reseñas", () => {
    const a = auditar(
      perfecta,
      { competidores: [], posicionEnBusqueda: 1 },
      { fichaReclamada: true, respondeResenas: false },
      AHORA,
    );
    expect(a.penalizacionManual).toBe(5);
    expect(a.puntajeTotal).toBe(95);
  });

  it("no penaliza lo que todavía no se verificó, pero lo declara", () => {
    const a = auditar(perfecta, { competidores: [], posicionEnBusqueda: 1 }, sinVerificar, AHORA);
    expect(a.penalizacionManual).toBe(0);
    expect(a.noEvaluados).toContain("manual_ficha_reclamada");
    expect(a.noEvaluados).toContain("manual_responde_resenas");
  });

  it("nunca baja de 0 aunque las penalizaciones se pasen", () => {
    const rota: FichaNegocio = {
      ...perfecta,
      rubro: "bakery",
      telefono: null,
      sitioWeb: null,
      diasConHorario: 0,
      cantidadFotos: 0,
      estadoOperativo: "cerrado_definitivo",
      puntaje: null,
      cantidadResenas: 0,
      fechaResenaMasReciente: null,
    };
    // Con rivales muy por encima, los tres bloques quedan casi en cero y las
    // penalizaciones manuales empujan el total por debajo de 0. Debe cortar en 0.
    const rival = (id: string, resenas: number): FichaNegocio => ({
      ...perfecta,
      idExterno: id,
      cantidadResenas: resenas,
    });
    const a = auditar(
      rota,
      {
        competidores: [rival("a", 300), rival("b", 200), rival("c", 150)],
        posicionEnBusqueda: "no_aparece",
      },
      { fichaReclamada: false, respondeResenas: false },
      AHORA,
    );
    expect(a.puntajeFicha).toBe(0);
    expect(a.puntajeReputacion).toBe(0);
    expect(a.penalizacionManual).toBe(15);
    expect(a.puntajeTotal).toBe(0);
  });

  it("los puntajes por bloque nunca son negativos", () => {
    const a = auditar(
      { ...perfecta, cantidadResenas: 0, puntaje: null, fechaResenaMasReciente: null },
      { competidores: [], posicionEnBusqueda: 1 },
      manualOk,
      AHORA,
    );
    expect(a.puntajeReputacion).toBe(0);
    expect(a.puntajeFicha).toBe(40);
  });

  it("arrastra el puesto y la brecha de reseñas al resultado", () => {
    const rival: FichaNegocio = { ...perfecta, idExterno: "r", cantidadResenas: 250 };
    const a = auditar(
      perfecta,
      { competidores: [rival], posicionEnBusqueda: 1 },
      manualOk,
      AHORA,
    );
    expect(a.puestoPorResenas).toBe(2);
    expect(a.totalEnRubro).toBe(2);
    expect(a.resenasParaSubirUnPuesto).toBe(51);
  });
});

describe("resumenDeVenta", () => {
  it("arma la frase con el puesto y la brecha", () => {
    const rival: FichaNegocio = { ...perfecta, idExterno: "r", cantidadResenas: 250 };
    const a = auditar(perfecta, { competidores: [rival], posicionEnBusqueda: 1 }, manualOk, AHORA);
    expect(resumenDeVenta(a)).toBe(
      "Estás 2º de 2 en tu rubro. Con 51 reseñas más pasás al puesto de arriba.",
    );
  });

  it("felicita al líder en vez de inventar una brecha", () => {
    const a = auditar(perfecta, { competidores: [], posicionEnBusqueda: 1 }, manualOk, AHORA);
    expect(resumenDeVenta(a)).toBe("Sos el mejor posicionado de tu rubro en la zona.");
  });
});
```

- [ ] **Step 2: Correr el test para verificar que falla**

```bash
npm test lib/seo-local/auditoria.test.ts
```

Esperado: FALLA por no poder resolver `@/lib/seo-local/auditoria`.

- [ ] **Step 3: Implementar el motor**

Crear `lib/seo-local/auditoria.ts`:

```ts
import { auditarFicha, PUNTOS_FICHA } from "@/lib/seo-local/reglas-ficha";
import { auditarReputacion, PUNTOS_REPUTACION } from "@/lib/seo-local/reglas-reputacion";
import { auditarCompetencia, PUNTOS_COMPETENCIA } from "@/lib/seo-local/reglas-competencia";
import type {
  Auditoria,
  ContextoCompetencia,
  FichaNegocio,
  Hallazgo,
  RevisionManual,
} from "@/lib/seo-local/tipos";

const PENALIZACION_NO_RECLAMADA = 10;
const PENALIZACION_NO_RESPONDE = 5;

const restar = (total: number, hallazgos: Hallazgo[]): number =>
  Math.max(0, total - hallazgos.reduce((s, h) => s + h.puntosPerdidos, 0));

/**
 * Motor de auditoría. Función pura: no toca red ni base de datos.
 *
 * Las penalizaciones manuales se restan del total de los tres bloques, con piso
 * en 0. Si algo no se verificó todavía, no se penaliza: se declara en
 * `noEvaluados` y el informe lo dice. Nunca se asume el peor ni el mejor caso.
 */
export function auditar(
  ficha: FichaNegocio,
  contexto: ContextoCompetencia,
  manual: RevisionManual,
  ahora: Date,
): Auditoria {
  const deFicha = auditarFicha(ficha);
  const deReputacion = auditarReputacion(ficha, ahora);
  const deCompetencia = auditarCompetencia(ficha, contexto);

  const hallazgos: Hallazgo[] = [
    ...deFicha,
    ...deReputacion.hallazgos,
    ...deCompetencia.hallazgos,
  ];
  const noEvaluados = [...deReputacion.noEvaluados, ...deCompetencia.noEvaluados];

  let penalizacionManual = 0;

  if (manual.fichaReclamada === null) {
    noEvaluados.push("manual_ficha_reclamada");
  } else if (!manual.fichaReclamada) {
    penalizacionManual += PENALIZACION_NO_RECLAMADA;
    hallazgos.push({
      codigo: "manual_ficha_reclamada",
      bloque: "manual",
      gravedad: "alta",
      puntosPerdidos: PENALIZACION_NO_RECLAMADA,
      titulo: "La ficha no está reclamada por el dueño",
      accion: "Reclamar la ficha en Google. Sin eso no controlás lo que Google muestra de tu negocio.",
    });
  }

  if (manual.respondeResenas === null) {
    noEvaluados.push("manual_responde_resenas");
  } else if (!manual.respondeResenas) {
    penalizacionManual += PENALIZACION_NO_RESPONDE;
    hallazgos.push({
      codigo: "manual_responde_resenas",
      bloque: "manual",
      gravedad: "media",
      puntosPerdidos: PENALIZACION_NO_RESPONDE,
      titulo: "No respondés las reseñas",
      accion: "Responder todas, sobre todo las malas. Google lo premia y los clientes lo leen.",
    });
  }

  const puntajeFicha = restar(PUNTOS_FICHA, deFicha);
  const puntajeReputacion = restar(PUNTOS_REPUTACION, deReputacion.hallazgos);
  const puntajeCompetencia = restar(PUNTOS_COMPETENCIA, deCompetencia.hallazgos);

  const puntajeTotal = Math.max(
    0,
    puntajeFicha + puntajeReputacion + puntajeCompetencia - penalizacionManual,
  );

  return {
    puntajeTotal,
    puntajeFicha,
    puntajeReputacion,
    puntajeCompetencia,
    penalizacionManual,
    hallazgos,
    noEvaluados,
    puestoPorResenas: deCompetencia.puestoPorResenas,
    totalEnRubro: deCompetencia.totalEnRubro,
    resenasParaSubirUnPuesto: deCompetencia.resenasParaSubirUnPuesto,
  };
}

/** La frase que se le dice al prospecto en la reunión. El puntaje es secundario. */
export function resumenDeVenta(auditoria: Auditoria): string {
  const { puestoPorResenas, totalEnRubro, resenasParaSubirUnPuesto } = auditoria;

  if (puestoPorResenas === null) {
    return "Todavía no hay competidores cargados de tu rubro en la zona para comparar.";
  }
  if (resenasParaSubirUnPuesto === null) {
    return "Sos el mejor posicionado de tu rubro en la zona.";
  }
  return `Estás ${puestoPorResenas}º de ${totalEnRubro} en tu rubro. Con ${resenasParaSubirUnPuesto} reseñas más pasás al puesto de arriba.`;
}
```

- [ ] **Step 4: Correr toda la batería de tests**

```bash
npm test
```

Esperado: PASA todo — 5 + 10 + 9 + 12 + 9 = 45 tests en 5 archivos.

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/Gesti/INICIATIVA GLOBAL/IG OS"
git add web/lib/seo-local/auditoria.ts web/lib/seo-local/auditoria.test.ts
git commit -m "SEO Local: motor de auditoría completo"
```

---

### Task 6: Esquema de base de datos

Tres tablas nuevas. Se definen en Drizzle (fuente de verdad del esquema) y se crean en Supabase con SQL, siguiendo el método ya usado en el proyecto.

**Files:**
- Modify: `lib/db/schema.ts` (agregar al final)
- Create: `docs/sql/2026-07-19-seo-local.sql` (relativo a la raíz del repo `IG OS/`, no a `web/`)

**Interfaces:**
- Consumes: `companies`, `leads` de `lib/db/schema.ts`
- Produces: tablas `seo_zonas`, `seo_negocios`, `seo_auditorias`

- [ ] **Step 1: Agregar las tablas al esquema de Drizzle**

Agregar al final de `lib/db/schema.ts`:

```ts
// ─── SEO Local ────────────────────────────────────────────────────────────────

// Zonas de barrido. Los puntos se calculan desde los extremos con puntosDeBarrido().
export const seoZonas = pgTable("seo_zonas", {
  id: uuid("id").defaultRandom().primaryKey(),
  nombre: text("nombre").notNull(),
  localidad: text("localidad").notNull().default(""),
  latA: numeric("lat_a").notNull(),
  lngA: numeric("lng_a").notNull(),
  latB: numeric("lat_b").notNull(),
  lngB: numeric("lng_b").notNull(),
  radioMetros: integer("radio_metros").notNull().default(350),
  ultimoBarrido: timestamp("ultimo_barrido", { withTimezone: true }),
  consultasGastadas: integer("consultas_gastadas").notNull().default(0),
  createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
});

// Negocios encontrados en un barrido. idExterno es el place id de Google.
export const seoNegocios = pgTable("seo_negocios", {
  id: uuid("id").defaultRandom().primaryKey(),
  zonaId: uuid("zona_id").notNull().references(() => seoZonas.id, { onDelete: "cascade" }),
  idExterno: text("id_externo").notNull().unique(),
  nombre: text("nombre").notNull(),
  rubro: text("rubro"),
  direccion: text("direccion").notNull().default(""),
  lat: numeric("lat").notNull(),
  lng: numeric("lng").notNull(),
  telefono: text("telefono"),
  sitioWeb: text("sitio_web"),
  diasConHorario: integer("dias_con_horario").notNull().default(0),
  puntaje: numeric("puntaje"),
  cantidadResenas: integer("cantidad_resenas").notNull().default(0),
  fechaResenaMasReciente: timestamp("fecha_resena_mas_reciente", { withTimezone: true }),
  cantidadFotos: integer("cantidad_fotos").notNull().default(0),
  estadoOperativo: text("estado_operativo").notNull().default("desconocido"),
  // Revisión manual: null = sin verificar
  fichaReclamada: boolean("ficha_reclamada"),
  respondeResenas: boolean("responde_resenas"),
  // Lead del CRM al que se convirtió, si se convirtió
  leadId: uuid("lead_id").references(() => leads.id, { onDelete: "set null" }),
  ultimaConsulta: timestamp("ultima_consulta", { withTimezone: true }).defaultNow().notNull(),
});

// Cada diagnóstico, guardado con fecha para poder mostrar el antes y después.
export const seoAuditorias = pgTable("seo_auditorias", {
  id: uuid("id").defaultRandom().primaryKey(),
  negocioId: uuid("negocio_id").notNull().references(() => seoNegocios.id, { onDelete: "cascade" }),
  puntajeTotal: integer("puntaje_total").notNull(),
  puntajeFicha: integer("puntaje_ficha").notNull(),
  puntajeReputacion: integer("puntaje_reputacion").notNull(),
  puntajeCompetencia: integer("puntaje_competencia").notNull(),
  penalizacionManual: integer("penalizacion_manual").notNull().default(0),
  hallazgos: jsonb("hallazgos").$type<unknown[]>().notNull().default([]),
  noEvaluados: jsonb("no_evaluados").$type<string[]>().notNull().default([]),
  puestoPorResenas: integer("puesto_por_resenas"),
  totalEnRubro: integer("total_en_rubro").notNull().default(1),
  resenasParaSubirUnPuesto: integer("resenas_para_subir_un_puesto"),
  createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
});
```

- [ ] **Step 2: Verificar que TypeScript compila**

```bash
cd "C:/Users/Gesti/INICIATIVA GLOBAL/IG OS/web"
npx tsc --noEmit
```

Esperado: sin errores. Si aparece un error por `boolean` no importado, verificar que la primera línea de `lib/db/schema.ts` incluya `boolean` en el import de `drizzle-orm/pg-core` (ya lo incluye).

- [ ] **Step 3: Escribir el SQL para Supabase**

Crear `docs/sql/2026-07-19-seo-local.sql` en la raíz del repo `IG OS/`:

```sql
-- SEO Local — tablas del módulo. Correr en el editor SQL de Supabase.

create table if not exists seo_zonas (
  id uuid primary key default gen_random_uuid(),
  nombre text not null,
  localidad text not null default '',
  lat_a numeric not null,
  lng_a numeric not null,
  lat_b numeric not null,
  lng_b numeric not null,
  radio_metros integer not null default 350,
  ultimo_barrido timestamptz,
  consultas_gastadas integer not null default 0,
  created_at timestamptz not null default now()
);

create table if not exists seo_negocios (
  id uuid primary key default gen_random_uuid(),
  zona_id uuid not null references seo_zonas(id) on delete cascade,
  id_externo text not null unique,
  nombre text not null,
  rubro text,
  direccion text not null default '',
  lat numeric not null,
  lng numeric not null,
  telefono text,
  sitio_web text,
  dias_con_horario integer not null default 0,
  puntaje numeric,
  cantidad_resenas integer not null default 0,
  fecha_resena_mas_reciente timestamptz,
  cantidad_fotos integer not null default 0,
  estado_operativo text not null default 'desconocido',
  ficha_reclamada boolean,
  responde_resenas boolean,
  lead_id uuid references leads(id) on delete set null,
  ultima_consulta timestamptz not null default now()
);

create index if not exists seo_negocios_zona_idx on seo_negocios(zona_id);
create index if not exists seo_negocios_rubro_idx on seo_negocios(rubro);

create table if not exists seo_auditorias (
  id uuid primary key default gen_random_uuid(),
  negocio_id uuid not null references seo_negocios(id) on delete cascade,
  puntaje_total integer not null,
  puntaje_ficha integer not null,
  puntaje_reputacion integer not null,
  puntaje_competencia integer not null,
  penalizacion_manual integer not null default 0,
  hallazgos jsonb not null default '[]'::jsonb,
  no_evaluados jsonb not null default '[]'::jsonb,
  puesto_por_resenas integer,
  total_en_rubro integer not null default 1,
  resenas_para_subir_un_puesto integer,
  created_at timestamptz not null default now()
);

create index if not exists seo_auditorias_negocio_idx on seo_auditorias(negocio_id, created_at desc);

-- RLS: solo usuarios autenticados. Sigue el criterio ya aplicado al resto de IG CRM,
-- donde el acceso se restringe por lista de emails permitidos en la capa de aplicación.
alter table seo_zonas enable row level security;
alter table seo_negocios enable row level security;
alter table seo_auditorias enable row level security;

create policy "seo_zonas autenticados" on seo_zonas
  for all to authenticated using (true) with check (true);
create policy "seo_negocios autenticados" on seo_negocios
  for all to authenticated using (true) with check (true);
create policy "seo_auditorias autenticados" on seo_auditorias
  for all to authenticated using (true) with check (true);

-- Zona inicial: tramo de Gorriti en Francisco Álvarez, entre Almafuerte y el Acceso Oeste.
insert into seo_zonas (nombre, localidad, lat_a, lng_a, lat_b, lng_b, radio_metros)
select 'Gorriti', 'Francisco Álvarez', -34.60208108, -58.86292683, -34.60989714, -58.86890736, 350
where not exists (select 1 from seo_zonas where nombre = 'Gorriti');
```

- [ ] **Step 4: Verificar las políticas RLS existentes antes de aplicar**

Antes de correr el SQL, revisar cómo quedaron las políticas del commit `960a8ab` para no contradecirlas:

```bash
cd "C:/Users/Gesti/INICIATIVA GLOBAL/IG OS"
git show 960a8ab --stat
```

Si ese commit definió un criterio de RLS distinto (por ejemplo, filtrado por `auth.uid()` o por email), ajustar las tres políticas del SQL para que sigan **el mismo criterio** en lugar de `using (true)`. Dejar constancia del criterio elegido en un comentario dentro del archivo SQL.

- [ ] **Step 5: Correr el SQL en Supabase**

Abrir el editor SQL del proyecto Supabase `zjetaihjddoxxvrpzwsb`, pegar el contenido de `docs/sql/2026-07-19-seo-local.sql` y ejecutar. Verificar en el explorador de tablas que aparecen `seo_zonas`, `seo_negocios` y `seo_auditorias`, y que `seo_zonas` tiene una fila con nombre "Gorriti".

- [ ] **Step 6: Commit**

```bash
cd "C:/Users/Gesti/INICIATIVA GLOBAL/IG OS"
git add web/lib/db/schema.ts docs/sql/2026-07-19-seo-local.sql
git commit -m "SEO Local: esquema de base de datos y zona inicial de Gorriti"
```

---

### Task 7: Proveedor de datos (Google Places)

La única pieza que habla con el mundo exterior. Se testea contra respuestas guardadas, sin consumir cuota.

**Files:**
- Create: `lib/seo-local/proveedor.ts` (interfaz y mapeo)
- Create: `lib/seo-local/proveedor-places.ts` (implementación Google)
- Create: `lib/seo-local/fixtures/places-nearby.json`
- Create: `lib/seo-local/fixtures/places-detalle.json`
- Test: `lib/seo-local/proveedor.test.ts`

**Interfaces:**
- Consumes: `Coordenada` de `@/lib/seo-local/geo`, `FichaNegocio` de `@/lib/seo-local/tipos`
- Produces:
  - `interface ProveedorSeoLocal` con `buscarEnZona`, `obtenerFicha`, `buscarPorTexto`
  - `function mapearLugarPlaces(lugar: LugarPlaces): FichaNegocio`
  - `function crearProveedorPlaces(apiKey: string): ProveedorSeoLocal`
  - `class ErrorProveedor extends Error` con propiedad `motivo: "sin_clave" | "cuota" | "red" | "respuesta"`

**Nota:** los nombres de campos y endpoints de Places (New) que aparecen abajo deben verificarse contra la documentación vigente antes de dar la tarea por terminada. Si alguno cambió, ajustar el mapeo y las fixtures juntos.

- [ ] **Step 1: Guardar las respuestas de ejemplo**

Crear `lib/seo-local/fixtures/places-nearby.json`:

```json
{
  "places": [
    {
      "id": "ChIJejemplo1",
      "displayName": { "text": "Panadería La Esquina" },
      "formattedAddress": "Gorriti 1150, Francisco Álvarez",
      "location": { "latitude": -34.6034, "longitude": -58.8639 },
      "primaryType": "bakery",
      "nationalPhoneNumber": "0237 400-1111",
      "websiteUri": "https://laesquina.com.ar",
      "regularOpeningHours": {
        "periods": [
          { "open": { "day": 1, "hour": 8, "minute": 0 } },
          { "open": { "day": 2, "hour": 8, "minute": 0 } },
          { "open": { "day": 3, "hour": 8, "minute": 0 } }
        ]
      },
      "rating": 4.3,
      "userRatingCount": 47,
      "photos": [{ "name": "p1" }, { "name": "p2" }],
      "businessStatus": "OPERATIONAL",
      "reviews": [{ "publishTime": "2026-06-30T12:00:00Z" }]
    },
    {
      "id": "ChIJejemplo2",
      "displayName": { "text": "Ferretería Gorriti" },
      "formattedAddress": "Gorriti 1400, Francisco Álvarez",
      "location": { "latitude": -34.6061, "longitude": -58.8661 },
      "businessStatus": "OPERATIONAL"
    }
  ]
}
```

Crear `lib/seo-local/fixtures/places-detalle.json`:

```json
{
  "id": "ChIJejemplo1",
  "displayName": { "text": "Panadería La Esquina" },
  "formattedAddress": "Gorriti 1150, Francisco Álvarez",
  "location": { "latitude": -34.6034, "longitude": -58.8639 },
  "primaryType": "bakery",
  "nationalPhoneNumber": "0237 400-1111",
  "websiteUri": "https://laesquina.com.ar",
  "regularOpeningHours": {
    "periods": [
      { "open": { "day": 0, "hour": 9, "minute": 0 } },
      { "open": { "day": 1, "hour": 8, "minute": 0 } },
      { "open": { "day": 2, "hour": 8, "minute": 0 } },
      { "open": { "day": 3, "hour": 8, "minute": 0 } },
      { "open": { "day": 4, "hour": 8, "minute": 0 } },
      { "open": { "day": 5, "hour": 8, "minute": 0 } },
      { "open": { "day": 6, "hour": 9, "minute": 0 } }
    ]
  },
  "rating": 4.3,
  "userRatingCount": 47,
  "photos": [{ "name": "p1" }, { "name": "p2" }, { "name": "p3" }, { "name": "p4" }, { "name": "p5" }],
  "businessStatus": "OPERATIONAL",
  "reviews": [
    { "publishTime": "2026-06-30T12:00:00Z" },
    { "publishTime": "2026-05-02T12:00:00Z" }
  ]
}
```

- [ ] **Step 2: Escribir el test que falla**

Crear `lib/seo-local/proveedor.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { mapearLugarPlaces, type LugarPlaces } from "@/lib/seo-local/proveedor";
import nearby from "@/lib/seo-local/fixtures/places-nearby.json";
import detalle from "@/lib/seo-local/fixtures/places-detalle.json";

describe("mapearLugarPlaces", () => {
  it("mapea una ficha completa", () => {
    const ficha = mapearLugarPlaces(detalle as LugarPlaces);
    expect(ficha.idExterno).toBe("ChIJejemplo1");
    expect(ficha.nombre).toBe("Panadería La Esquina");
    expect(ficha.rubro).toBe("bakery");
    expect(ficha.direccion).toBe("Gorriti 1150, Francisco Álvarez");
    expect(ficha.ubicacion).toEqual({ lat: -34.6034, lng: -58.8639 });
    expect(ficha.telefono).toBe("0237 400-1111");
    expect(ficha.sitioWeb).toBe("https://laesquina.com.ar");
    expect(ficha.puntaje).toBe(4.3);
    expect(ficha.cantidadResenas).toBe(47);
    expect(ficha.cantidadFotos).toBe(5);
    expect(ficha.estadoOperativo).toBe("abierto");
  });

  it("cuenta días distintos con horario, no periodos", () => {
    expect(mapearLugarPlaces(detalle as LugarPlaces).diasConHorario).toBe(7);
    const parcial = (nearby.places as LugarPlaces[])[0];
    expect(mapearLugarPlaces(parcial).diasConHorario).toBe(3);
  });

  it("toma la fecha de la reseña más reciente", () => {
    const ficha = mapearLugarPlaces(detalle as LugarPlaces);
    expect(ficha.fechaResenaMasReciente).toBe("2026-06-30T12:00:00.000Z");
  });

  it("no inventa datos cuando la ficha viene vacía", () => {
    const pelada = (nearby.places as LugarPlaces[])[1];
    const ficha = mapearLugarPlaces(pelada);
    expect(ficha.telefono).toBeNull();
    expect(ficha.sitioWeb).toBeNull();
    expect(ficha.rubro).toBeNull();
    expect(ficha.puntaje).toBeNull();
    expect(ficha.cantidadResenas).toBe(0);
    expect(ficha.cantidadFotos).toBe(0);
    expect(ficha.diasConHorario).toBe(0);
    expect(ficha.fechaResenaMasReciente).toBeNull();
  });

  it("traduce los estados de negocio de Google", () => {
    const cerrado = { ...(detalle as LugarPlaces), businessStatus: "CLOSED_PERMANENTLY" as const };
    expect(mapearLugarPlaces(cerrado).estadoOperativo).toBe("cerrado_definitivo");
    const temporal = { ...(detalle as LugarPlaces), businessStatus: "CLOSED_TEMPORARILY" as const };
    expect(mapearLugarPlaces(temporal).estadoOperativo).toBe("cerrado_temporal");
    const sinDato = { ...(detalle as LugarPlaces), businessStatus: undefined };
    expect(mapearLugarPlaces(sinDato).estadoOperativo).toBe("desconocido");
  });
});
```

- [ ] **Step 3: Correr el test para verificar que falla**

```bash
npm test lib/seo-local/proveedor.test.ts
```

Esperado: FALLA por no poder resolver `@/lib/seo-local/proveedor`.

- [ ] **Step 4: Implementar la interfaz y el mapeo**

Crear `lib/seo-local/proveedor.ts`:

```ts
import type { Coordenada } from "@/lib/seo-local/geo";
import type { FichaNegocio } from "@/lib/seo-local/tipos";

/** Forma cruda de un lugar en Places API (New). Solo los campos que pedimos. */
export type LugarPlaces = {
  id: string;
  displayName?: { text?: string };
  formattedAddress?: string;
  location?: { latitude: number; longitude: number };
  primaryType?: string;
  nationalPhoneNumber?: string;
  websiteUri?: string;
  regularOpeningHours?: { periods?: { open?: { day?: number } }[] };
  rating?: number;
  userRatingCount?: number;
  photos?: { name?: string }[];
  businessStatus?: "OPERATIONAL" | "CLOSED_TEMPORARILY" | "CLOSED_PERMANENTLY";
  reviews?: { publishTime?: string }[];
};

export type MotivoError = "sin_clave" | "cuota" | "red" | "respuesta";

export class ErrorProveedor extends Error {
  constructor(public motivo: MotivoError, mensaje: string) {
    super(mensaje);
    this.name = "ErrorProveedor";
  }
}

/**
 * Única puerta de salida hacia datos de SEO local. Cambiar de Google a
 * DataForSEO es escribir otra implementación de esta interfaz: nada más
 * del sistema conoce el formato de respuesta de ningún proveedor.
 */
export interface ProveedorSeoLocal {
  buscarEnZona(centro: Coordenada, radioMetros: number): Promise<FichaNegocio[]>;
  obtenerFicha(idExterno: string): Promise<FichaNegocio>;
  buscarPorTexto(consulta: string, sesgo: Coordenada, radioMetros: number): Promise<FichaNegocio[]>;
}

const textoONull = (v: string | undefined): string | null => (v && v.trim() !== "" ? v : null);

function traducirEstado(estado: LugarPlaces["businessStatus"]): FichaNegocio["estadoOperativo"] {
  switch (estado) {
    case "OPERATIONAL":
      return "abierto";
    case "CLOSED_TEMPORARILY":
      return "cerrado_temporal";
    case "CLOSED_PERMANENTLY":
      return "cerrado_definitivo";
    default:
      return "desconocido";
  }
}

/** Convierte la respuesta cruda de Google al vocabulario del módulo. */
export function mapearLugarPlaces(lugar: LugarPlaces): FichaNegocio {
  // Google puede mandar varios periodos por día (mañana y tarde), así que se
  // cuentan días distintos, no cantidad de periodos.
  const dias = new Set(
    (lugar.regularOpeningHours?.periods ?? [])
      .map((p) => p.open?.day)
      .filter((d): d is number => typeof d === "number"),
  );

  const fechas = (lugar.reviews ?? [])
    .map((r) => r.publishTime)
    .filter((f): f is string => typeof f === "string")
    .map((f) => new Date(f).getTime())
    .filter((t) => !Number.isNaN(t));

  return {
    idExterno: lugar.id,
    nombre: lugar.displayName?.text ?? "(sin nombre)",
    rubro: textoONull(lugar.primaryType),
    direccion: lugar.formattedAddress ?? "",
    ubicacion: {
      lat: lugar.location?.latitude ?? 0,
      lng: lugar.location?.longitude ?? 0,
    },
    telefono: textoONull(lugar.nationalPhoneNumber),
    sitioWeb: textoONull(lugar.websiteUri),
    diasConHorario: dias.size,
    puntaje: typeof lugar.rating === "number" ? lugar.rating : null,
    cantidadResenas: lugar.userRatingCount ?? 0,
    fechaResenaMasReciente:
      fechas.length > 0 ? new Date(Math.max(...fechas)).toISOString() : null,
    cantidadFotos: lugar.photos?.length ?? 0,
    estadoOperativo: traducirEstado(lugar.businessStatus),
  };
}
```

- [ ] **Step 5: Correr el test para verificar que pasa**

```bash
npm test lib/seo-local/proveedor.test.ts
```

Esperado: PASA, 5 tests.

Si falla la importación del JSON, verificar que `tsconfig.json` tenga `"resolveJsonModule": true` (ya lo tiene).

- [ ] **Step 6: Implementar el cliente de Google Places**

Crear `lib/seo-local/proveedor-places.ts`:

```ts
import "server-only";
import type { Coordenada } from "@/lib/seo-local/geo";
import type { FichaNegocio } from "@/lib/seo-local/tipos";
import {
  ErrorProveedor,
  mapearLugarPlaces,
  type LugarPlaces,
  type ProveedorSeoLocal,
} from "@/lib/seo-local/proveedor";

const BASE = "https://places.googleapis.com/v1";

/** Campos que pedimos. Places cobra por máscara, así que no se pide de más. */
const CAMPOS = [
  "id",
  "displayName",
  "formattedAddress",
  "location",
  "primaryType",
  "nationalPhoneNumber",
  "websiteUri",
  "regularOpeningHours",
  "rating",
  "userRatingCount",
  "photos",
  "businessStatus",
  "reviews",
];

const mascaraLista = CAMPOS.map((c) => `places.${c}`).join(",");
const mascaraDetalle = CAMPOS.join(",");

/** Places devuelve como máximo 20 por consulta. */
const MAX_RESULTADOS = 20;

async function pedir<T>(url: string, apiKey: string, mascara: string, body?: unknown): Promise<T> {
  let respuesta: Response;
  try {
    respuesta = await fetch(url, {
      method: body ? "POST" : "GET",
      headers: {
        "Content-Type": "application/json",
        "X-Goog-Api-Key": apiKey,
        "X-Goog-FieldMask": mascara,
      },
      body: body ? JSON.stringify(body) : undefined,
      cache: "no-store",
    });
  } catch (e) {
    throw new ErrorProveedor("red", `No se pudo contactar a Google: ${(e as Error).message}`);
  }

  if (respuesta.status === 429) {
    throw new ErrorProveedor("cuota", "Se agotó la cuota de consultas de Google Places.");
  }
  if (respuesta.status === 401 || respuesta.status === 403) {
    throw new ErrorProveedor(
      "sin_clave",
      "Google rechazó la clave. Revisá GOOGLE_PLACES_API_KEY y que la Places API (New) esté habilitada.",
    );
  }
  if (!respuesta.ok) {
    throw new ErrorProveedor("respuesta", `Google respondió ${respuesta.status}.`);
  }

  return (await respuesta.json()) as T;
}

export function crearProveedorPlaces(apiKey: string): ProveedorSeoLocal {
  if (!apiKey || apiKey.trim() === "") {
    throw new ErrorProveedor("sin_clave", "Falta GOOGLE_PLACES_API_KEY en web/.env.local.");
  }

  return {
    async buscarEnZona(centro: Coordenada, radioMetros: number): Promise<FichaNegocio[]> {
      const datos = await pedir<{ places?: LugarPlaces[] }>(
        `${BASE}/places:searchNearby`,
        apiKey,
        mascaraLista,
        {
          maxResultCount: MAX_RESULTADOS,
          languageCode: "es",
          regionCode: "AR",
          locationRestriction: {
            circle: {
              center: { latitude: centro.lat, longitude: centro.lng },
              radius: radioMetros,
            },
          },
        },
      );
      return (datos.places ?? []).map(mapearLugarPlaces);
    },

    async obtenerFicha(idExterno: string): Promise<FichaNegocio> {
      const datos = await pedir<LugarPlaces>(
        `${BASE}/places/${encodeURIComponent(idExterno)}?languageCode=es&regionCode=AR`,
        apiKey,
        mascaraDetalle,
      );
      return mapearLugarPlaces(datos);
    },

    async buscarPorTexto(
      consulta: string,
      sesgo: Coordenada,
      radioMetros: number,
    ): Promise<FichaNegocio[]> {
      const datos = await pedir<{ places?: LugarPlaces[] }>(
        `${BASE}/places:searchText`,
        apiKey,
        mascaraLista,
        {
          textQuery: consulta,
          maxResultCount: MAX_RESULTADOS,
          languageCode: "es",
          regionCode: "AR",
          locationBias: {
            circle: {
              center: { latitude: sesgo.lat, longitude: sesgo.lng },
              radius: radioMetros,
            },
          },
        },
      );
      return (datos.places ?? []).map(mapearLugarPlaces);
    },
  };
}

/** Lee la clave del entorno. Devuelve null si no está configurada. */
export function proveedorDesdeEntorno(): ProveedorSeoLocal | null {
  const clave = process.env.GOOGLE_PLACES_API_KEY;
  if (!clave || clave.trim() === "") return null;
  return crearProveedorPlaces(clave);
}
```

- [ ] **Step 7: Instalar `server-only` y verificar la compilación**

```bash
npm install server-only
npx tsc --noEmit
npm test
```

Esperado: `tsc` sin errores y los 50 tests pasando.

- [ ] **Step 8: Verificar los nombres de campos contra la documentación vigente**

Abrir la documentación de Places API (New) y confirmar: el endpoint `places:searchNearby`, el encabezado `X-Goog-FieldMask`, y los nombres `displayName`, `primaryType`, `nationalPhoneNumber`, `websiteUri`, `regularOpeningHours.periods[].open.day`, `userRatingCount`, `businessStatus`, `reviews[].publishTime`.

Si alguno cambió: corregir `LugarPlaces` y `mapearLugarPlaces` en `proveedor.ts`, actualizar las dos fixtures para que reflejen la forma real, y volver a correr `npm test`.

- [ ] **Step 9: Commit**

```bash
cd "C:/Users/Gesti/INICIATIVA GLOBAL/IG OS"
git add web/package.json web/package-lock.json web/lib/seo-local/proveedor.ts web/lib/seo-local/proveedor-places.ts web/lib/seo-local/proveedor.test.ts web/lib/seo-local/fixtures
git commit -m "SEO Local: proveedor de datos con Google Places"
```

---

### Task 8: Barrido de zona y persistencia

**Files:**
- Create: `lib/seo-local/datos.ts` (lecturas desde Supabase)
- Create: `app/(app)/seo-local/actions.ts` (server actions)

**Interfaces:**
- Consumes: `puntosDeBarrido` de `@/lib/seo-local/geo`, `proveedorDesdeEntorno` de `@/lib/seo-local/proveedor-places`, `auditar`/`resumenDeVenta` de `@/lib/seo-local/auditoria`
- Produces:
  - `type ZonaFila`, `type NegocioFila` (en `datos.ts`)
  - `function listarZonas(): Promise<ZonaFila[]>`
  - `function obtenerZona(id: string): Promise<ZonaFila | null>`
  - `function listarNegociosDeZona(zonaId: string): Promise<NegocioFila[]>`
  - `function obtenerNegocio(id: string): Promise<NegocioFila | null>`
  - `function filaAFicha(fila: NegocioFila): FichaNegocio`
  - `function hayClaveConfigurada(): boolean`
  - `async function barrerZona(formData: FormData): Promise<void>` (server action)
  - `async function guardarRevisionManual(formData: FormData): Promise<void>` (server action)
  - `async function correrAuditoria(formData: FormData): Promise<void>` (server action)

- [ ] **Step 1: Escribir la capa de lectura**

Crear `lib/seo-local/datos.ts`:

```ts
import { createClient } from "@/lib/supabase/server";
import type { FichaNegocio } from "@/lib/seo-local/tipos";
import type { Coordenada } from "@/lib/seo-local/geo";

export type ZonaFila = {
  id: string;
  nombre: string;
  localidad: string;
  a: Coordenada;
  b: Coordenada;
  radioMetros: number;
  ultimoBarrido: string | null;
  consultasGastadas: number;
};

export type NegocioFila = {
  id: string;
  zonaId: string;
  idExterno: string;
  nombre: string;
  rubro: string | null;
  direccion: string;
  lat: number;
  lng: number;
  telefono: string | null;
  sitioWeb: string | null;
  diasConHorario: number;
  puntaje: number | null;
  cantidadResenas: number;
  fechaResenaMasReciente: string | null;
  cantidadFotos: number;
  estadoOperativo: FichaNegocio["estadoOperativo"];
  fichaReclamada: boolean | null;
  respondeResenas: boolean | null;
  leadId: string | null;
  ultimaConsulta: string;
};

/* eslint-disable @typescript-eslint/no-explicit-any */
const aZona = (r: any): ZonaFila => ({
  id: r.id,
  nombre: r.nombre,
  localidad: r.localidad ?? "",
  a: { lat: Number(r.lat_a), lng: Number(r.lng_a) },
  b: { lat: Number(r.lat_b), lng: Number(r.lng_b) },
  radioMetros: Number(r.radio_metros),
  ultimoBarrido: r.ultimo_barrido ?? null,
  consultasGastadas: Number(r.consultas_gastadas ?? 0),
});

const aNegocio = (r: any): NegocioFila => ({
  id: r.id,
  zonaId: r.zona_id,
  idExterno: r.id_externo,
  nombre: r.nombre,
  rubro: r.rubro ?? null,
  direccion: r.direccion ?? "",
  lat: Number(r.lat),
  lng: Number(r.lng),
  telefono: r.telefono ?? null,
  sitioWeb: r.sitio_web ?? null,
  diasConHorario: Number(r.dias_con_horario ?? 0),
  puntaje: r.puntaje === null || r.puntaje === undefined ? null : Number(r.puntaje),
  cantidadResenas: Number(r.cantidad_resenas ?? 0),
  fechaResenaMasReciente: r.fecha_resena_mas_reciente ?? null,
  cantidadFotos: Number(r.cantidad_fotos ?? 0),
  estadoOperativo: r.estado_operativo ?? "desconocido",
  fichaReclamada: r.ficha_reclamada ?? null,
  respondeResenas: r.responde_resenas ?? null,
  leadId: r.lead_id ?? null,
  ultimaConsulta: r.ultima_consulta,
});
/* eslint-enable @typescript-eslint/no-explicit-any */

/** Convierte una fila de la base al vocabulario del motor de auditoría. */
export function filaAFicha(fila: NegocioFila): FichaNegocio {
  return {
    idExterno: fila.idExterno,
    nombre: fila.nombre,
    rubro: fila.rubro,
    direccion: fila.direccion,
    ubicacion: { lat: fila.lat, lng: fila.lng },
    telefono: fila.telefono,
    sitioWeb: fila.sitioWeb,
    diasConHorario: fila.diasConHorario,
    puntaje: fila.puntaje,
    cantidadResenas: fila.cantidadResenas,
    fechaResenaMasReciente: fila.fechaResenaMasReciente,
    cantidadFotos: fila.cantidadFotos,
    estadoOperativo: fila.estadoOperativo,
  };
}

export function hayClaveConfigurada(): boolean {
  const c = process.env.GOOGLE_PLACES_API_KEY;
  return typeof c === "string" && c.trim() !== "";
}

export async function listarZonas(): Promise<ZonaFila[]> {
  const sb = await createClient();
  const { data } = await sb.from("seo_zonas").select("*").order("nombre");
  return (data ?? []).map(aZona);
}

export async function obtenerZona(id: string): Promise<ZonaFila | null> {
  const sb = await createClient();
  const { data } = await sb.from("seo_zonas").select("*").eq("id", id).single();
  return data ? aZona(data) : null;
}

export async function listarNegociosDeZona(zonaId: string): Promise<NegocioFila[]> {
  const sb = await createClient();
  const { data } = await sb
    .from("seo_negocios")
    .select("*")
    .eq("zona_id", zonaId)
    .order("cantidad_resenas", { ascending: true });
  return (data ?? []).map(aNegocio);
}

export async function obtenerNegocio(id: string): Promise<NegocioFila | null> {
  const sb = await createClient();
  const { data } = await sb.from("seo_negocios").select("*").eq("id", id).single();
  return data ? aNegocio(data) : null;
}

/** Última auditoría guardada de un negocio, si existe. */
export async function ultimaAuditoria(negocioId: string) {
  const sb = await createClient();
  const { data } = await sb
    .from("seo_auditorias")
    .select("*")
    .eq("negocio_id", negocioId)
    .order("created_at", { ascending: false })
    .limit(1)
    .maybeSingle();
  return data;
}
```

- [ ] **Step 2: Escribir las server actions**

Crear `app/(app)/seo-local/actions.ts`:

```ts
"use server";
import { revalidatePath } from "next/cache";
import { createClient } from "@/lib/supabase/server";
import { puntosDeBarrido } from "@/lib/seo-local/geo";
import { proveedorDesdeEntorno } from "@/lib/seo-local/proveedor-places";
import { ErrorProveedor } from "@/lib/seo-local/proveedor";
import { auditar } from "@/lib/seo-local/auditoria";
import {
  filaAFicha,
  listarNegociosDeZona,
  obtenerNegocio,
  obtenerZona,
} from "@/lib/seo-local/datos";
import type { PosicionBusqueda } from "@/lib/seo-local/tipos";

/**
 * Barre una zona: consulta Google en cada punto, deduplica y persiste.
 * Si Google falla, no se borra nada: quedan los datos del barrido anterior.
 */
export async function barrerZona(formData: FormData): Promise<void> {
  const zonaId = String(formData.get("zonaId") ?? "");
  if (!zonaId) return;

  const zona = await obtenerZona(zonaId);
  if (!zona) return;

  const proveedor = proveedorDesdeEntorno();
  if (!proveedor) return;

  const puntos = puntosDeBarrido(zona.a, zona.b, zona.radioMetros);
  const encontrados = new Map<string, Awaited<ReturnType<typeof proveedor.obtenerFicha>>>();
  let consultas = 0;

  try {
    for (const punto of puntos) {
      const fichas = await proveedor.buscarEnZona(punto, zona.radioMetros);
      consultas += 1;
      for (const f of fichas) encontrados.set(f.idExterno, f);
    }
  } catch (e) {
    if (!(e instanceof ErrorProveedor)) throw e;
    // Con datos parciales igual se persiste lo que se llegó a traer.
  }

  const sb = await createClient();

  if (encontrados.size > 0) {
    await sb.from("seo_negocios").upsert(
      [...encontrados.values()].map((f) => ({
        zona_id: zonaId,
        id_externo: f.idExterno,
        nombre: f.nombre,
        rubro: f.rubro,
        direccion: f.direccion,
        lat: f.ubicacion.lat,
        lng: f.ubicacion.lng,
        telefono: f.telefono,
        sitio_web: f.sitioWeb,
        dias_con_horario: f.diasConHorario,
        puntaje: f.puntaje,
        cantidad_resenas: f.cantidadResenas,
        fecha_resena_mas_reciente: f.fechaResenaMasReciente,
        cantidad_fotos: f.cantidadFotos,
        estado_operativo: f.estadoOperativo,
        ultima_consulta: new Date().toISOString(),
      })),
      { onConflict: "id_externo" },
    );
  }

  await sb
    .from("seo_zonas")
    .update({
      ultimo_barrido: new Date().toISOString(),
      consultas_gastadas: zona.consultasGastadas + consultas,
    })
    .eq("id", zonaId);

  revalidatePath(`/seo-local/${zonaId}`);
  revalidatePath("/seo-local");
}

/** Guarda las dos verificaciones que Google no informa. */
export async function guardarRevisionManual(formData: FormData): Promise<void> {
  const negocioId = String(formData.get("negocioId") ?? "");
  if (!negocioId) return;

  const aBooleano = (v: FormDataEntryValue | null): boolean | null => {
    const s = String(v ?? "");
    if (s === "si") return true;
    if (s === "no") return false;
    return null;
  };

  const sb = await createClient();
  await sb
    .from("seo_negocios")
    .update({
      ficha_reclamada: aBooleano(formData.get("fichaReclamada")),
      responde_resenas: aBooleano(formData.get("respondeResenas")),
    })
    .eq("id", negocioId);

  revalidatePath(`/seo-local/negocio/${negocioId}`);
}

/**
 * Corre el diagnóstico y lo guarda con fecha.
 *
 * Refresca la ficha desde Google si hay clave; si no la hay o Google falla,
 * audita con los datos persistidos. Un diagnóstico con datos de ayer es
 * infinitamente mejor que una pantalla de error en medio de una reunión.
 */
export async function correrAuditoria(formData: FormData): Promise<void> {
  const negocioId = String(formData.get("negocioId") ?? "");
  if (!negocioId) return;

  const negocio = await obtenerNegocio(negocioId);
  if (!negocio) return;

  const sb = await createClient();
  const proveedor = proveedorDesdeEntorno();
  let ficha = filaAFicha(negocio);
  let posicionEnBusqueda: PosicionBusqueda = "no_evaluado";

  if (proveedor) {
    try {
      ficha = await proveedor.obtenerFicha(negocio.idExterno);
      await sb
        .from("seo_negocios")
        .update({
          telefono: ficha.telefono,
          sitio_web: ficha.sitioWeb,
          dias_con_horario: ficha.diasConHorario,
          puntaje: ficha.puntaje,
          cantidad_resenas: ficha.cantidadResenas,
          fecha_resena_mas_reciente: ficha.fechaResenaMasReciente,
          cantidad_fotos: ficha.cantidadFotos,
          estado_operativo: ficha.estadoOperativo,
          ultima_consulta: new Date().toISOString(),
        })
        .eq("id", negocioId);

      // Posición en la búsqueda "rubro + localidad".
      const zona = await obtenerZona(negocio.zonaId);
      if (zona && ficha.rubro) {
        const resultados = await proveedor.buscarPorTexto(
          `${ficha.rubro} ${zona.localidad || zona.nombre}`,
          zona.a,
          zona.radioMetros,
        );
        const indice = resultados.findIndex((r) => r.idExterno === ficha.idExterno);
        posicionEnBusqueda = indice === -1 ? "no_aparece" : indice + 1;
      }
    } catch (e) {
      if (!(e instanceof ErrorProveedor)) throw e;
      // Se sigue con los datos persistidos.
    }
  }

  const todos = await listarNegociosDeZona(negocio.zonaId);
  const competidores = todos
    .filter((n) => n.id !== negocio.id && n.rubro !== null && n.rubro === ficha.rubro)
    .map(filaAFicha);

  const auditoria = auditar(
    ficha,
    { competidores, posicionEnBusqueda },
    { fichaReclamada: negocio.fichaReclamada, respondeResenas: negocio.respondeResenas },
    new Date(),
  );

  await sb.from("seo_auditorias").insert({
    negocio_id: negocioId,
    puntaje_total: auditoria.puntajeTotal,
    puntaje_ficha: auditoria.puntajeFicha,
    puntaje_reputacion: auditoria.puntajeReputacion,
    puntaje_competencia: auditoria.puntajeCompetencia,
    penalizacion_manual: auditoria.penalizacionManual,
    hallazgos: auditoria.hallazgos,
    no_evaluados: auditoria.noEvaluados,
    puesto_por_resenas: auditoria.puestoPorResenas,
    total_en_rubro: auditoria.totalEnRubro,
    resenas_para_subir_un_puesto: auditoria.resenasParaSubirUnPuesto,
  });

  revalidatePath(`/seo-local/negocio/${negocioId}`);
}
```

- [ ] **Step 3: Verificar la compilación**

```bash
cd "C:/Users/Gesti/INICIATIVA GLOBAL/IG OS/web"
npx tsc --noEmit
```

Esperado: sin errores.

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/Gesti/INICIATIVA GLOBAL/IG OS"
git add "web/lib/seo-local/datos.ts" "web/app/(app)/seo-local/actions.ts"
git commit -m "SEO Local: barrido de zona y persistencia"
```

---

### Task 9: Pantalla de zonas y tabla de prospectos

**Files:**
- Create: `app/(app)/seo-local/page.tsx` (listado de zonas)
- Create: `app/(app)/seo-local/[zonaId]/page.tsx` (tabla de prospectos)
- Create: `components/seo-local/aviso-sin-clave.tsx`

**Interfaces:**
- Consumes: `listarZonas`, `obtenerZona`, `listarNegociosDeZona`, `hayClaveConfigurada` de `@/lib/seo-local/datos`; `barrerZona` de `@/app/(app)/seo-local/actions`; `puntosDeBarrido`, `distanciaMetros` de `@/lib/seo-local/geo`
- Produces: rutas `/seo-local` y `/seo-local/[zonaId]`; componente `AvisoSinClave`

- [ ] **Step 1: Crear el aviso de clave faltante**

Crear `components/seo-local/aviso-sin-clave.tsx`:

```tsx
import { KeyRound } from "lucide-react";

export function AvisoSinClave() {
  return (
    <div
      style={{
        display: "flex",
        gap: 12,
        alignItems: "flex-start",
        background: "var(--card)",
        border: "1px solid #FF996655",
        borderRadius: 14,
        padding: "14px 16px",
        marginBottom: 18,
      }}
    >
      <KeyRound size={18} style={{ color: "#FF9966", flexShrink: 0, marginTop: 2 }} />
      <div>
        <div style={{ fontSize: 13.5, fontWeight: 650, marginBottom: 4 }}>
          Falta configurar la clave de Google Places
        </div>
        <p style={{ fontSize: 12.5, color: "var(--muted)", lineHeight: 1.6, margin: 0 }}>
          El módulo funciona con los datos ya guardados, pero no puede traer información nueva.
          Agregá <code>GOOGLE_PLACES_API_KEY</code> en <code>web/.env.local</code> y reiniciá el
          servidor.
        </p>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Crear el listado de zonas**

Crear `app/(app)/seo-local/page.tsx`:

```tsx
import Link from "next/link";
import { MapPin, Radar } from "lucide-react";
import { listarZonas, hayClaveConfigurada } from "@/lib/seo-local/datos";
import { distanciaMetros, puntosDeBarrido } from "@/lib/seo-local/geo";
import { AvisoSinClave } from "@/components/seo-local/aviso-sin-clave";

const fecha = (iso: string | null) =>
  iso ? new Date(iso).toLocaleDateString("es-AR", { day: "2-digit", month: "short" }) : "nunca";

export default async function ZonasPage() {
  const zonas = await listarZonas();
  const conClave = hayClaveConfigurada();

  return (
    <div style={{ padding: 30, maxWidth: 1100 }}>
      <header style={{ marginBottom: 22 }}>
        <h1 style={{ fontSize: 24, fontWeight: 760, letterSpacing: "-.5px", margin: "0 0 6px" }}>
          SEO Local
        </h1>
        <p style={{ fontSize: 13.5, color: "var(--muted)", margin: 0 }}>
          Barré una zona para encontrar comercios mal posicionados en Google. Los peores son tus
          mejores prospectos.
        </p>
      </header>

      {!conClave && <AvisoSinClave />}

      {zonas.length === 0 && (
        <p style={{ fontSize: 13.5, color: "var(--muted)" }}>
          Todavía no hay zonas cargadas. Corré el SQL de <code>docs/sql/2026-07-19-seo-local.sql</code>{" "}
          en Supabase para crear la zona de Gorriti.
        </p>
      )}

      <div style={{ display: "grid", gap: 12 }}>
        {zonas.map((z) => {
          const largo = Math.round(distanciaMetros(z.a, z.b));
          const puntos = puntosDeBarrido(z.a, z.b, z.radioMetros).length;
          return (
            <Link
              key={z.id}
              href={`/seo-local/${z.id}`}
              style={{
                display: "flex",
                alignItems: "center",
                gap: 14,
                background: "var(--card)",
                border: "1px solid var(--border)",
                borderRadius: 14,
                padding: "16px 18px",
              }}
            >
              <div
                style={{
                  width: 42,
                  height: 42,
                  borderRadius: 12,
                  background: "var(--grad)",
                  display: "grid",
                  placeItems: "center",
                  color: "#fff",
                  flexShrink: 0,
                }}
              >
                <MapPin size={19} />
              </div>
              <div style={{ flex: 1 }}>
                <div style={{ fontSize: 15, fontWeight: 680 }}>
                  {z.nombre}
                  {z.localidad && (
                    <span style={{ color: "var(--muted)", fontWeight: 500 }}> · {z.localidad}</span>
                  )}
                </div>
                <div style={{ fontSize: 12.5, color: "var(--muted)", marginTop: 3 }}>
                  {largo} m · {puntos} puntos de barrido · último barrido: {fecha(z.ultimoBarrido)}
                </div>
              </div>
              <Radar size={17} style={{ color: "var(--muted)" }} />
            </Link>
          );
        })}
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Crear la tabla de prospectos**

Crear `app/(app)/seo-local/[zonaId]/page.tsx`:

```tsx
import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, Radar } from "lucide-react";
import { obtenerZona, listarNegociosDeZona, hayClaveConfigurada } from "@/lib/seo-local/datos";
import { barrerZona } from "@/app/(app)/seo-local/actions";
import { AvisoSinClave } from "@/components/seo-local/aviso-sin-clave";

/** Prioridad de prospección: cuanto peor está, más arriba aparece. */
function urgencia(n: { cantidadResenas: number; sitioWeb: string | null; telefono: string | null; diasConHorario: number; puntaje: number | null }) {
  let p = 0;
  if (n.cantidadResenas < 10) p += 3;
  if (!n.sitioWeb) p += 2;
  if (!n.telefono) p += 2;
  if (n.diasConHorario < 7) p += 2;
  if (n.puntaje !== null && n.puntaje < 4) p += 2;
  return p;
}

const celda: React.CSSProperties = {
  padding: "11px 12px",
  fontSize: 13,
  borderBottom: "1px solid var(--border)",
};

export default async function ZonaPage({ params }: { params: Promise<{ zonaId: string }> }) {
  const { zonaId } = await params;
  const zona = await obtenerZona(zonaId);
  if (!zona) notFound();

  const negocios = (await listarNegociosDeZona(zonaId)).sort((a, b) => urgencia(b) - urgencia(a));
  const conClave = hayClaveConfigurada();

  return (
    <div style={{ padding: 30, maxWidth: 1180 }}>
      <Link
        href="/seo-local"
        style={{ display: "inline-flex", alignItems: "center", gap: 7, fontSize: 13, color: "var(--muted)", marginBottom: 14 }}
      >
        <ArrowLeft size={15} /> Zonas
      </Link>

      <header style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16, marginBottom: 20 }}>
        <div>
          <h1 style={{ fontSize: 23, fontWeight: 760, letterSpacing: "-.5px", margin: "0 0 5px" }}>
            {zona.nombre}
            {zona.localidad && <span style={{ color: "var(--muted)", fontWeight: 500 }}> · {zona.localidad}</span>}
          </h1>
          <p style={{ fontSize: 12.5, color: "var(--muted)", margin: 0 }}>
            {negocios.length} negocios · {zona.consultasGastadas} consultas gastadas
          </p>
        </div>

        <form action={barrerZona}>
          <input type="hidden" name="zonaId" value={zona.id} />
          <button
            type="submit"
            disabled={!conClave}
            style={{
              display: "inline-flex",
              alignItems: "center",
              gap: 8,
              background: conClave ? "var(--grad)" : "var(--card)",
              color: conClave ? "#fff" : "var(--muted)",
              border: conClave ? "none" : "1px solid var(--border)",
              borderRadius: 11,
              padding: "11px 18px",
              fontSize: 13.5,
              fontWeight: 640,
              cursor: conClave ? "pointer" : "not-allowed",
            }}
          >
            <Radar size={16} /> Barrer zona
          </button>
        </form>
      </header>

      {!conClave && <AvisoSinClave />}

      {negocios.length === 0 ? (
        <p style={{ fontSize: 13.5, color: "var(--muted)" }}>
          Todavía no barriste esta zona. Apretá <strong>Barrer zona</strong> para traer los negocios
          desde Google.
        </p>
      ) : (
        <div style={{ background: "var(--card)", border: "1px solid var(--border)", borderRadius: 14, overflow: "hidden" }}>
          <table style={{ width: "100%", borderCollapse: "collapse" }}>
            <thead>
              <tr style={{ textAlign: "left", fontSize: 11.5, color: "var(--muted)", textTransform: "uppercase", letterSpacing: ".5px" }}>
                <th style={{ ...celda, fontWeight: 600 }}>Negocio</th>
                <th style={{ ...celda, fontWeight: 600 }}>Rubro</th>
                <th style={{ ...celda, fontWeight: 600 }}>Reseñas</th>
                <th style={{ ...celda, fontWeight: 600 }}>Puntaje</th>
                <th style={{ ...celda, fontWeight: 600 }}>Falta</th>
              </tr>
            </thead>
            <tbody>
              {negocios.map((n) => {
                const faltantes = [
                  !n.telefono && "teléfono",
                  !n.sitioWeb && "web",
                  n.diasConHorario < 7 && "horarios",
                  n.cantidadFotos < 5 && "fotos",
                ].filter(Boolean) as string[];
                return (
                  <tr key={n.id}>
                    <td style={celda}>
                      <Link href={`/seo-local/negocio/${n.id}`} style={{ fontWeight: 620 }}>
                        {n.nombre}
                      </Link>
                      <div style={{ fontSize: 11.5, color: "var(--muted)", marginTop: 2 }}>{n.direccion}</div>
                    </td>
                    <td style={{ ...celda, color: "var(--muted)" }}>{n.rubro ?? "—"}</td>
                    <td style={celda}>{n.cantidadResenas}</td>
                    <td style={celda}>{n.puntaje === null ? "—" : n.puntaje.toFixed(1)}</td>
                    <td style={{ ...celda, color: faltantes.length ? "#FF9966" : "var(--muted)" }}>
                      {faltantes.length ? faltantes.join(", ") : "nada"}
                    </td>
                  </tr>
                );
              })}
            </tbody>
          </table>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 4: Verificar en el navegador**

```bash
cd "C:/Users/Gesti/INICIATIVA GLOBAL/IG OS/web"
npm run dev
```

Abrir `http://localhost:3001/seo-local`. Esperado: se ve la zona "Gorriti · Francisco Álvarez" con el largo del tramo y "3 puntos de barrido". Al entrar, la tabla vacía con el botón **Barrer zona**. Si no hay clave configurada, el aviso naranja y el botón deshabilitado.

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/Gesti/INICIATIVA GLOBAL/IG OS"
git add "web/app/(app)/seo-local" "web/components/seo-local"
git commit -m "SEO Local: pantallas de zonas y prospectos"
```

---

### Task 10: Pantalla de auditoría

**Files:**
- Create: `app/(app)/seo-local/negocio/[negocioId]/page.tsx`

**Interfaces:**
- Consumes: `obtenerNegocio`, `ultimaAuditoria` de `@/lib/seo-local/datos`; `correrAuditoria`, `guardarRevisionManual` de `@/app/(app)/seo-local/actions`; `resumenDeVenta` de `@/lib/seo-local/auditoria`
- Produces: ruta `/seo-local/negocio/[negocioId]`

- [ ] **Step 1: Crear la pantalla**

Crear `app/(app)/seo-local/negocio/[negocioId]/page.tsx`:

```tsx
import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, FileText, RefreshCw } from "lucide-react";
import { obtenerNegocio, ultimaAuditoria } from "@/lib/seo-local/datos";
import { correrAuditoria, guardarRevisionManual } from "@/app/(app)/seo-local/actions";
import { resumenDeVenta } from "@/lib/seo-local/auditoria";
import type { Auditoria, Hallazgo } from "@/lib/seo-local/tipos";

const colorPuntaje = (p: number) => (p >= 70 ? "#4ade80" : p >= 40 ? "#FF9966" : "#FF6B6B");

const tarjeta: React.CSSProperties = {
  background: "var(--card)",
  border: "1px solid var(--border)",
  borderRadius: 14,
  padding: 18,
};

function Selector({ nombre, etiqueta, valor }: { nombre: string; etiqueta: string; valor: boolean | null }) {
  const actual = valor === null ? "" : valor ? "si" : "no";
  return (
    <label style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12, fontSize: 13 }}>
      <span>{etiqueta}</span>
      <select
        name={nombre}
        defaultValue={actual}
        style={{
          background: "var(--card)",
          color: "inherit",
          border: "1px solid var(--border)",
          borderRadius: 9,
          padding: "7px 10px",
          fontSize: 12.5,
        }}
      >
        <option value="">Sin verificar</option>
        <option value="si">Sí</option>
        <option value="no">No</option>
      </select>
    </label>
  );
}

export default async function NegocioPage({ params }: { params: Promise<{ negocioId: string }> }) {
  const { negocioId } = await params;
  const negocio = await obtenerNegocio(negocioId);
  if (!negocio) notFound();

  const fila = await ultimaAuditoria(negocioId);
  const auditoria: Auditoria | null = fila
    ? {
        puntajeTotal: fila.puntaje_total,
        puntajeFicha: fila.puntaje_ficha,
        puntajeReputacion: fila.puntaje_reputacion,
        puntajeCompetencia: fila.puntaje_competencia,
        penalizacionManual: fila.penalizacion_manual,
        hallazgos: fila.hallazgos as Hallazgo[],
        noEvaluados: fila.no_evaluados as string[],
        puestoPorResenas: fila.puesto_por_resenas,
        totalEnRubro: fila.total_en_rubro,
        resenasParaSubirUnPuesto: fila.resenas_para_subir_un_puesto,
      }
    : null;

  return (
    <div style={{ padding: 30, maxWidth: 920 }}>
      <Link
        href={`/seo-local/${negocio.zonaId}`}
        style={{ display: "inline-flex", alignItems: "center", gap: 7, fontSize: 13, color: "var(--muted)", marginBottom: 14 }}
      >
        <ArrowLeft size={15} /> Volver a la zona
      </Link>

      <header style={{ marginBottom: 20 }}>
        <h1 style={{ fontSize: 24, fontWeight: 760, letterSpacing: "-.5px", margin: "0 0 5px" }}>
          {negocio.nombre}
        </h1>
        <p style={{ fontSize: 12.5, color: "var(--muted)", margin: 0 }}>
          {negocio.direccion} · {negocio.rubro ?? "sin rubro"} · datos del{" "}
          {new Date(negocio.ultimaConsulta).toLocaleDateString("es-AR")}
        </p>
      </header>

      <div style={{ display: "grid", gap: 14, gridTemplateColumns: "1fr 1fr", marginBottom: 14 }}>
        <form action={guardarRevisionManual} style={{ ...tarjeta, display: "grid", gap: 12 }}>
          <input type="hidden" name="negocioId" value={negocio.id} />
          <div style={{ fontSize: 13.5, fontWeight: 660 }}>Revisión manual</div>
          <p style={{ fontSize: 12, color: "var(--muted)", lineHeight: 1.55, margin: 0 }}>
            Google no informa esto por API. Abrí la ficha y verificalo a ojo.
          </p>
          <Selector nombre="fichaReclamada" etiqueta="¿Ficha reclamada por el dueño?" valor={negocio.fichaReclamada} />
          <Selector nombre="respondeResenas" etiqueta="¿Responde las reseñas?" valor={negocio.respondeResenas} />
          <button
            type="submit"
            style={{
              background: "var(--card)",
              border: "1px solid var(--border)",
              borderRadius: 10,
              padding: "9px 14px",
              fontSize: 12.5,
              fontWeight: 620,
              color: "inherit",
              cursor: "pointer",
            }}
          >
            Guardar
          </button>
        </form>

        <div style={{ ...tarjeta, display: "grid", gap: 12, alignContent: "start" }}>
          <div style={{ fontSize: 13.5, fontWeight: 660 }}>Diagnóstico</div>
          {auditoria ? (
            <>
              <div style={{ display: "flex", alignItems: "baseline", gap: 8 }}>
                <span style={{ fontSize: 40, fontWeight: 780, color: colorPuntaje(auditoria.puntajeTotal), letterSpacing: "-1.5px" }}>
                  {auditoria.puntajeTotal}
                </span>
                <span style={{ fontSize: 13, color: "var(--muted)" }}>/ 100 de visibilidad local</span>
              </div>
              <p style={{ fontSize: 13, lineHeight: 1.6, margin: 0 }}>{resumenDeVenta(auditoria)}</p>
            </>
          ) : (
            <p style={{ fontSize: 12.5, color: "var(--muted)", margin: 0 }}>
              Todavía no corriste el diagnóstico de este negocio.
            </p>
          )}

          <div style={{ display: "flex", gap: 8 }}>
            <form action={correrAuditoria}>
              <input type="hidden" name="negocioId" value={negocio.id} />
              <button
                type="submit"
                style={{
                  display: "inline-flex",
                  alignItems: "center",
                  gap: 7,
                  background: "var(--grad)",
                  color: "#fff",
                  border: "none",
                  borderRadius: 10,
                  padding: "10px 15px",
                  fontSize: 12.5,
                  fontWeight: 640,
                  cursor: "pointer",
                }}
              >
                <RefreshCw size={14} /> {auditoria ? "Volver a auditar" : "Auditar"}
              </button>
            </form>
            {auditoria && (
              <Link
                href={`/seo-local/informe/${negocio.id}`}
                style={{
                  display: "inline-flex",
                  alignItems: "center",
                  gap: 7,
                  background: "var(--card)",
                  border: "1px solid var(--border)",
                  borderRadius: 10,
                  padding: "10px 15px",
                  fontSize: 12.5,
                  fontWeight: 620,
                }}
              >
                <FileText size={14} /> Ver informe
              </Link>
            )}
          </div>
        </div>
      </div>

      {auditoria && auditoria.hallazgos.length > 0 && (
        <div style={tarjeta}>
          <div style={{ fontSize: 13.5, fontWeight: 660, marginBottom: 12 }}>
            {auditoria.hallazgos.length} problemas detectados
          </div>
          <div style={{ display: "grid", gap: 10 }}>
            {auditoria.hallazgos.map((h) => (
              <div key={h.codigo} style={{ display: "flex", gap: 11, alignItems: "flex-start" }}>
                <span
                  style={{
                    width: 8,
                    height: 8,
                    borderRadius: 99,
                    background: h.gravedad === "alta" ? "#FF6B6B" : h.gravedad === "media" ? "#FF9966" : "#5b9dff",
                    marginTop: 6,
                    flexShrink: 0,
                  }}
                />
                <div style={{ flex: 1 }}>
                  <div style={{ fontSize: 13, fontWeight: 620 }}>{h.titulo}</div>
                  <div style={{ fontSize: 12.5, color: "var(--muted)", marginTop: 2, lineHeight: 1.55 }}>{h.accion}</div>
                </div>
                <span style={{ fontSize: 12, color: "var(--muted)", flexShrink: 0 }}>−{h.puntosPerdidos}</span>
              </div>
            ))}
          </div>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Verificar en el navegador**

Con `npm run dev` corriendo, entrar a un negocio desde la tabla de la zona. Esperado: se ve la tarjeta de revisión manual con los dos selectores en "Sin verificar", y el botón **Auditar**. Al apretarlo (con clave configurada), aparece el puntaje, la frase de venta y la lista de hallazgos.

- [ ] **Step 3: Commit**

```bash
cd "C:/Users/Gesti/INICIATIVA GLOBAL/IG OS"
git add "web/app/(app)/seo-local/negocio"
git commit -m "SEO Local: pantalla de auditoría con revisión manual"
```

---

### Task 11: Informe imprimible

La página que se convierte en el PDF que se manda por WhatsApp. Fuera del grupo `(app)` para que no arrastre la barra lateral.

**Files:**
- Create: `app/seo-local/informe/[negocioId]/page.tsx`
- Create: `app/seo-local/informe/[negocioId]/informe.css`
- Create: `components/seo-local/boton-imprimir.tsx`

**Nota de rutas:** la pantalla de auditoría enlaza a `/seo-local/informe/<id>`. Esta ruta vive en `app/seo-local/...` (sin el grupo `(app)`), y los grupos de rutas de Next.js no aparecen en la URL, así que el enlace resuelve acá sin cambios.

**Interfaces:**
- Consumes: `obtenerNegocio`, `ultimaAuditoria` de `@/lib/seo-local/datos`; `resumenDeVenta` de `@/lib/seo-local/auditoria`
- Produces: ruta `/seo-local/informe/[negocioId]`; componente `BotonImprimir`

- [ ] **Step 1: Crear el botón de impresión**

Crear `components/seo-local/boton-imprimir.tsx`:

```tsx
"use client";
import { Printer } from "lucide-react";

export function BotonImprimir() {
  return (
    <button type="button" className="no-imprimir" onClick={() => window.print()}>
      <Printer size={15} /> Guardar como PDF
    </button>
  );
}
```

- [ ] **Step 2: Crear los estilos del informe**

Crear `app/seo-local/informe/[negocioId]/informe.css`:

```css
.informe {
  max-width: 780px;
  margin: 0 auto;
  padding: 40px 32px 60px;
  color: #0B0B0D;
  background: #fff;
  font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", sans-serif;
}

.informe-marca {
  display: flex;
  align-items: center;
  justify-content: space-between;
  border-bottom: 2px solid #0B0B0D;
  padding-bottom: 14px;
  margin-bottom: 26px;
}

.informe-marca strong {
  font-size: 17px;
  letter-spacing: -0.4px;
}

.informe-marca span {
  font-size: 12px;
  color: #666;
}

.informe h1 {
  font-size: 27px;
  letter-spacing: -0.7px;
  margin: 0 0 5px;
}

.informe-sub {
  font-size: 13px;
  color: #666;
  margin: 0 0 26px;
}

.informe-puntaje {
  display: flex;
  align-items: center;
  gap: 20px;
  border: 2px solid #0B0B0D;
  border-radius: 14px;
  padding: 20px 24px;
  margin-bottom: 12px;
}

.informe-puntaje b {
  font-size: 52px;
  letter-spacing: -2px;
  line-height: 1;
}

.informe-resumen {
  font-size: 15px;
  line-height: 1.55;
  font-weight: 600;
  margin: 0 0 26px;
}

.informe-bloques {
  display: flex;
  gap: 10px;
  margin-bottom: 26px;
}

.informe-bloque {
  flex: 1;
  border: 1px solid #ddd;
  border-radius: 10px;
  padding: 12px 14px;
}

.informe-bloque em {
  display: block;
  font-style: normal;
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: #666;
  margin-bottom: 4px;
}

.informe-bloque b {
  font-size: 20px;
}

.informe h2 {
  font-size: 15px;
  margin: 0 0 12px;
  padding-bottom: 7px;
  border-bottom: 1px solid #ddd;
}

.informe-hallazgo {
  display: flex;
  gap: 11px;
  padding: 11px 0;
  border-bottom: 1px solid #eee;
  page-break-inside: avoid;
}

.informe-punto {
  width: 9px;
  height: 9px;
  border-radius: 99px;
  flex-shrink: 0;
  margin-top: 5px;
}

.informe-hallazgo b {
  display: block;
  font-size: 13.5px;
  margin-bottom: 2px;
}

.informe-hallazgo span {
  font-size: 12.5px;
  color: #555;
  line-height: 1.5;
}

.informe-nota {
  font-size: 11.5px;
  color: #777;
  line-height: 1.55;
  margin-top: 26px;
  padding-top: 14px;
  border-top: 1px solid #ddd;
}

.no-imprimir {
  display: inline-flex;
  align-items: center;
  gap: 7px;
  background: #0B0B0D;
  color: #fff;
  border: none;
  border-radius: 10px;
  padding: 11px 18px;
  font-size: 13px;
  font-weight: 620;
  cursor: pointer;
  margin-bottom: 20px;
}

@media print {
  .no-imprimir {
    display: none;
  }
  .informe {
    padding: 0;
    max-width: none;
  }
}
```

- [ ] **Step 3: Crear la página del informe**

Crear `app/seo-local/informe/[negocioId]/page.tsx`:

```tsx
import { notFound } from "next/navigation";
import { obtenerNegocio, ultimaAuditoria } from "@/lib/seo-local/datos";
import { resumenDeVenta } from "@/lib/seo-local/auditoria";
import { BotonImprimir } from "@/components/seo-local/boton-imprimir";
import type { Auditoria, Hallazgo } from "@/lib/seo-local/tipos";
import "./informe.css";

const colorGravedad = (g: Hallazgo["gravedad"]) =>
  g === "alta" ? "#d92d20" : g === "media" ? "#e08020" : "#3b7ddd";

export default async function InformePage({ params }: { params: Promise<{ negocioId: string }> }) {
  const { negocioId } = await params;
  const negocio = await obtenerNegocio(negocioId);
  if (!negocio) notFound();

  const fila = await ultimaAuditoria(negocioId);
  if (!fila) notFound();

  const auditoria: Auditoria = {
    puntajeTotal: fila.puntaje_total,
    puntajeFicha: fila.puntaje_ficha,
    puntajeReputacion: fila.puntaje_reputacion,
    puntajeCompetencia: fila.puntaje_competencia,
    penalizacionManual: fila.penalizacion_manual,
    hallazgos: fila.hallazgos as Hallazgo[],
    noEvaluados: fila.no_evaluados as string[],
    puestoPorResenas: fila.puesto_por_resenas,
    totalEnRubro: fila.total_en_rubro,
    resenasParaSubirUnPuesto: fila.resenas_para_subir_un_puesto,
  };

  const fecha = new Date(fila.created_at).toLocaleDateString("es-AR", {
    day: "2-digit",
    month: "long",
    year: "numeric",
  });

  return (
    <div className="informe">
      <BotonImprimir />

      <div className="informe-marca">
        <strong>INICIATIVA GLOBAL</strong>
        <span>Diagnóstico de posicionamiento local · {fecha}</span>
      </div>

      <h1>{negocio.nombre}</h1>
      <p className="informe-sub">
        {negocio.direccion}
        {negocio.rubro && ` · ${negocio.rubro}`}
      </p>

      <div className="informe-puntaje">
        <b style={{ color: colorGravedad(auditoria.puntajeTotal >= 70 ? "baja" : auditoria.puntajeTotal >= 40 ? "media" : "alta") }}>
          {auditoria.puntajeTotal}
        </b>
        <div>
          <div style={{ fontSize: 13, color: "#666" }}>de 100 puntos de visibilidad local</div>
          <div style={{ fontSize: 12.5, color: "#666", marginTop: 3 }}>
            Ficha {auditoria.puntajeFicha}/40 · Reputación {auditoria.puntajeReputacion}/30 ·
            Competencia {auditoria.puntajeCompetencia}/30
          </div>
        </div>
      </div>

      <p className="informe-resumen">{resumenDeVenta(auditoria)}</p>

      <h2>Qué encontramos</h2>
      {auditoria.hallazgos.length === 0 ? (
        <p style={{ fontSize: 13.5 }}>
          No encontramos problemas en tu presencia local. Está muy bien trabajada.
        </p>
      ) : (
        auditoria.hallazgos.map((h) => (
          <div key={h.codigo} className="informe-hallazgo">
            <span className="informe-punto" style={{ background: colorGravedad(h.gravedad) }} />
            <div>
              <b>{h.titulo}</b>
              <span>{h.accion}</span>
            </div>
          </div>
        ))
      )}

      {auditoria.noEvaluados.length > 0 && (
        <p className="informe-nota">
          Hay {auditoria.noEvaluados.length} punto{auditoria.noEvaluados.length === 1 ? "" : "s"} que
          no pudimos evaluar por falta de datos públicos. No los contamos ni a favor ni en contra del
          puntaje.
        </p>
      )}

      <p className="informe-nota">
        Diagnóstico elaborado por Iniciativa Global a partir de información pública de Google al{" "}
        {fecha}. Los datos de posicionamiento cambian con el tiempo.
      </p>
    </div>
  );
}
```

- [ ] **Step 4: Verificar la impresión**

Con `npm run dev` corriendo, abrir `/seo-local/informe/<id>` de un negocio ya auditado. Esperado: página blanca con la marca INICIATIVA GLOBAL, el puntaje grande y los hallazgos. Apretar **Guardar como PDF**: el diálogo de impresión debe mostrar la página **sin** el botón y sin la barra lateral.

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/Gesti/INICIATIVA GLOBAL/IG OS"
git add web/app/seo-local web/components/seo-local/boton-imprimir.tsx
git commit -m "SEO Local: informe imprimible con marca IG"
```

---

### Task 12: Puente al embudo y entrada en el menú

Cierra el circuito: de prospecto detectado a lead del CRM. Y hace visible el módulo.

**Files:**
- Modify: `app/(app)/seo-local/actions.ts` (agregar acción al final)
- Modify: `app/(app)/seo-local/negocio/[negocioId]/page.tsx` (agregar el formulario)
- Modify: `components/shell/sidebar.tsx` (agregar la entrada de menú)

**Interfaces:**
- Consumes: `obtenerNegocio` de `@/lib/seo-local/datos`; tabla `companies` y `leads` del esquema existente
- Produces: `async function pasarALead(formData: FormData): Promise<void>` (server action)

- [ ] **Step 1: Agregar la acción de conversión**

Agregar al final de `app/(app)/seo-local/actions.ts`:

```ts
/**
 * Convierte un negocio auditado en lead del embudo de IG CRM.
 *
 * El lead se crea en la empresa que se elija y en su primera etapa. Guarda el
 * vínculo en seo_negocios.lead_id para no duplicar si se aprieta dos veces.
 */
export async function pasarALead(formData: FormData): Promise<void> {
  const negocioId = String(formData.get("negocioId") ?? "");
  const companySlug = String(formData.get("companySlug") ?? "");
  if (!negocioId || !companySlug) return;

  const negocio = await obtenerNegocio(negocioId);
  if (!negocio || negocio.leadId) return;

  const sb = await createClient();
  const { data: empresa } = await sb.from("companies").select("id").eq("slug", companySlug).single();
  if (!empresa) return;

  const { data: etapa } = await sb
    .from("stages")
    .select("id")
    .eq("company_id", empresa.id)
    .order("order", { ascending: true })
    .limit(1)
    .maybeSingle();

  const { data: lead } = await sb
    .from("leads")
    .insert({
      company_id: empresa.id,
      stage_id: etapa?.id ?? null,
      name: negocio.nombre,
      description: `SEO Local · ${negocio.direccion}`,
      value: 0,
      channel: "otro",
      notes: negocio.telefono ? `Teléfono: ${negocio.telefono}` : "",
    })
    .select("id")
    .single();

  if (lead) {
    await sb.from("seo_negocios").update({ lead_id: lead.id }).eq("id", negocioId);
  }

  revalidatePath(`/seo-local/negocio/${negocioId}`);
  revalidatePath(`/empresas/${companySlug}`);
}
```

- [ ] **Step 2: Agregar el formulario a la pantalla de auditoría**

En `app/(app)/seo-local/negocio/[negocioId]/page.tsx`, agregar a los imports existentes:

```tsx
import { pasarALead } from "@/app/(app)/seo-local/actions";
import { companies } from "@/lib/companies";
```

Y agregar este bloque justo antes del cierre `</div>` final del componente (después del bloque de hallazgos):

```tsx
      <div style={{ ...tarjeta, marginTop: 14 }}>
        <div style={{ fontSize: 13.5, fontWeight: 660, marginBottom: 4 }}>Pasar al embudo</div>
        {negocio.leadId ? (
          <p style={{ fontSize: 12.5, color: "var(--muted)", margin: 0 }}>
            Ya está cargado como lead en el CRM.
          </p>
        ) : (
          <form action={pasarALead} style={{ display: "flex", gap: 8, alignItems: "center", marginTop: 10 }}>
            <input type="hidden" name="negocioId" value={negocio.id} />
            <select
              name="companySlug"
              defaultValue={companies[0]?.slug ?? ""}
              style={{
                background: "var(--card)",
                color: "inherit",
                border: "1px solid var(--border)",
                borderRadius: 9,
                padding: "9px 11px",
                fontSize: 12.5,
              }}
            >
              {companies.map((c) => (
                <option key={c.slug} value={c.slug}>
                  {c.name}
                </option>
              ))}
            </select>
            <button
              type="submit"
              style={{
                background: "var(--card)",
                border: "1px solid var(--border)",
                borderRadius: 10,
                padding: "9px 15px",
                fontSize: 12.5,
                fontWeight: 620,
                color: "inherit",
                cursor: "pointer",
              }}
            >
              Crear lead
            </button>
          </form>
        )}
      </div>
```

- [ ] **Step 3: Agregar la entrada al menú lateral**

En `components/shell/sidebar.tsx`, agregar `Search` a la lista de iconos importados desde `lucide-react`, y agregar esta línea al array `nav`, justo después de la entrada de `/marketing` (línea 27):

```ts
  { href: "/seo-local", label: "SEO Local", icon: Search, badge: "Nuevo" },
```

- [ ] **Step 4: Verificar la compilación y los tests**

```bash
cd "C:/Users/Gesti/INICIATIVA GLOBAL/IG OS/web"
npx tsc --noEmit
npm test
npm run lint
```

Esperado: `tsc` sin errores, 50 tests pasando, lint limpio.

- [ ] **Step 5: Verificar el circuito completo en el navegador**

Con `npm run dev`:

1. La barra lateral muestra **SEO Local** con la insignia "Nuevo".
2. `/seo-local` → zona Gorriti → **Barrer zona** → aparecen negocios reales del tramo.
3. Entrar a un negocio → **Auditar** → puntaje, frase de venta y hallazgos.
4. Marcar las dos casillas de revisión manual → **Guardar** → volver a auditar → el puntaje baja según corresponda.
5. **Ver informe** → **Guardar como PDF** → el PDF sale limpio.
6. **Crear lead** → abrir `/empresas/<slug>` → el negocio aparece en la primera etapa del embudo.

- [ ] **Step 6: Commit**

```bash
cd "C:/Users/Gesti/INICIATIVA GLOBAL/IG OS"
git add "web/app/(app)/seo-local/actions.ts" "web/app/(app)/seo-local/negocio/[negocioId]/page.tsx" web/components/shell/sidebar.tsx
git commit -m "SEO Local: puente al embudo y entrada en el menú"
```

---

## Verificación final

Al terminar las 12 tareas, el módulo debe cumplir:

- [ ] `npm test` pasa (50 tests en 6 archivos)
- [ ] `npx tsc --noEmit` sin errores
- [ ] `npm run lint` limpio
- [ ] El barrido de Gorriti trae negocios reales del tramo
- [ ] Un negocio sin ficha completa produce hallazgos concretos y accionables
- [ ] El informe se imprime a PDF sin la barra lateral ni el botón
- [ ] Un negocio auditado se convierte en lead del embudo
- [ ] Sin `GOOGLE_PLACES_API_KEY`, el módulo sigue navegable con datos guardados y avisa con claridad
