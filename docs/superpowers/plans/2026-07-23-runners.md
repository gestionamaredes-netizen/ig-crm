# Runners (gestiones y pagos) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sumar al módulo Cambio un subsistema de runners: registrar las gestiones (retiros/transferencias) que hace cada runner, su pago por gestión (precargado por cuenta), y los pagos que se le hacen, mostrando el saldo pendiente por runner.

**Architecture:** Cuatro tablas nuevas en Supabase (`runners`, `runner_accounts`, `runner_gestiones`, `runner_payments`), una función de cálculo pura, capa de datos de solo-lectura, server actions de alta, y una página `/cambio/runners`. Todo aislado del cálculo del cambio: las gestiones/pagos NO tocan stock, costo promedio, margen ni saldos de cajas.

**Tech Stack:** Next.js 16 App Router, React 19, TypeScript, Supabase (PostgREST + RLS + Auth), Vitest.

## Global Constraints

- Empresa objetivo en SQL: `where c.name ilike '%gestiones%ma%'`. Proyecto Supabase `zjetaihjddoxxvrpzwsb`.
- SQL crudo idempotente en `web/lib/db/sql/2026-07-23-runners.sql`; se corre a mano.
- RLS: misma plantilla del cambio → `for all to authenticated using (true) with check (true)`.
- Todas las tablas: `id uuid pk default gen_random_uuid()`, `company_id uuid not null references companies(id) on delete cascade`, `created_at timestamptz not null default now()`.
- El **pago (fee)** al runner y los **pagos** están en **pesos**.
- Montos: `parsearMonto` de `@/lib/finanzas/montos` para entrada, `formatearPesos` de `@/lib/formato` para salida.
- Server actions: resultado discriminado `type ResultadoAlta = { ok: true } | { ok: false; error: string }`, `revalidatePath("/cambio/runners")` en su propio try/catch, log con prefijo `[cambio]`, SIN candado `tieneAccesoCompleto`.
- Lecturas por `createClient()` de `@/lib/supabase/server`; el límite anti-truncado es `const LIMITE = 10000` con aviso por `console.error` si `count` supera lo devuelto.
- Fechas `YYYY-MM-DD`; `hoyISO()` ya existe en `lib/cambio/datos.ts` (re-export de finanzas).
- Estilos inline con CSS variables; acento dorado (heredado de la página); mobile-first con las clases `.ops-cards`/`.ops-tabla` y `.campo-fila`/`.cambio-modal` ya definidas en `web/app/globals.css`.
- El módulo es cambio-exclusivo; `/cambio/runners` cae bajo `/cambio/*`, ya permitido para el tier `cambio` en `canAccessPath`.

---

### Task 1: Esquema SQL

**Files:**
- Create: `web/lib/db/sql/2026-07-23-runners.sql`

**Interfaces:**
- Produces: tablas `runners`, `runner_accounts`, `runner_gestiones`, `runner_payments` con las columnas del diseño.

- [ ] **Step 1: Escribir el SQL idempotente**

Seguir el estilo de `web/lib/db/sql/2026-07-22-cambio.sql` (comentarios, `create table if not exists`, índices únicos, RLS con `drop policy if exists` + `create policy`, y verificación al final). Contenido:

```sql
-- Runners (gestiones y pagos) — módulo Cambio
-- Correr en el editor SQL de Supabase (proyecto zjetaihjddoxxvrpzwsb), editor
-- VACÍO antes de pegar. Idempotente.

-- 1. Runners (personas de logística)
create table if not exists runners (
  id         uuid primary key default gen_random_uuid(),
  company_id uuid not null references companies(id) on delete cascade,
  name       text not null,
  active     boolean not null default true,
  created_at timestamptz not null default now()
);
create unique index if not exists runners_name_idx on runners(company_id, lower(name));

-- 2. Cuentas de gestión (con el pago al runner por gestión, en pesos)
create table if not exists runner_accounts (
  id         uuid primary key default gen_random_uuid(),
  company_id uuid not null references companies(id) on delete cascade,
  name       text not null,
  currency   text not null,               -- ARS | USD (lo que se mueve)
  fee        numeric not null default 0,  -- pago al runner por gestión, en pesos
  active     boolean not null default true,
  created_at timestamptz not null default now()
);
create unique index if not exists runner_accounts_name_idx on runner_accounts(company_id, lower(name));

-- 3. Gestiones (cada retiro/transferencia que hace un runner)
create table if not exists runner_gestiones (
  id           uuid primary key default gen_random_uuid(),
  company_id   uuid not null references companies(id) on delete cascade,
  gestion_date date not null,
  runner_id    uuid not null references runners(id) on delete restrict,
  account_id   uuid not null references runner_accounts(id) on delete restrict,
  kind         text not null,               -- retiro | transferencia
  amount       numeric not null default 0,  -- monto movido, solo informativo
  fee          numeric not null default 0,  -- pago de esta gestión, en pesos
  notes        text not null default '',
  created_at   timestamptz not null default now()
);
create index if not exists runner_gestiones_runner_idx on runner_gestiones(company_id, runner_id);
create index if not exists runner_gestiones_date_idx on runner_gestiones(company_id, gestion_date);

-- 4. Pagos a runners (aparte: bajan el saldo)
create table if not exists runner_payments (
  id           uuid primary key default gen_random_uuid(),
  company_id   uuid not null references companies(id) on delete cascade,
  payment_date date not null,
  runner_id    uuid not null references runners(id) on delete restrict,
  amount       numeric not null default 0,  -- pesos
  notes        text not null default '',
  created_at   timestamptz not null default now()
);
create index if not exists runner_payments_runner_idx on runner_payments(company_id, runner_id);

-- 5. RLS (misma plantilla que el resto del cambio)
alter table runners          enable row level security;
alter table runner_accounts  enable row level security;
alter table runner_gestiones enable row level security;
alter table runner_payments  enable row level security;

drop policy if exists "auth_all_runners" on runners;
create policy "auth_all_runners" on runners for all to authenticated using (true) with check (true);
drop policy if exists "auth_all_runner_accounts" on runner_accounts;
create policy "auth_all_runner_accounts" on runner_accounts for all to authenticated using (true) with check (true);
drop policy if exists "auth_all_runner_gestiones" on runner_gestiones;
create policy "auth_all_runner_gestiones" on runner_gestiones for all to authenticated using (true) with check (true);
drop policy if exists "auth_all_runner_payments" on runner_payments;
create policy "auth_all_runner_payments" on runner_payments for all to authenticated using (true) with check (true);

-- 6. Runners iniciales de Gestiones MA
insert into runners (company_id, name)
select c.id, v.name
from companies c
cross join (values ('Owen'), ('Zurdo'), ('Capi')) as v(name)
where c.name ilike '%gestiones%ma%'
on conflict do nothing;

-- 7. Verificación
-- Tiene que devolver 3 filas: Owen, Zurdo, Capi.
select r.name from runners r join companies c on c.id = r.company_id
where c.name ilike '%gestiones%ma%' order by r.name;
-- Las cuatro tablas con RLS activo.
select tablename, rowsecurity from pg_tables
where tablename in ('runners','runner_accounts','runner_gestiones','runner_payments');
```

- [ ] **Step 2: Commit**

```bash
git add web/lib/db/sql/2026-07-23-runners.sql
git commit -m "feat(cambio): esquema SQL de runners (gestiones y pagos)"
```

---

### Task 2: Tipos y cálculo puro

**Files:**
- Create: `web/lib/cambio/runners.ts`
- Test: `web/lib/cambio/runners.test.ts`

**Interfaces:**
- Produces:
  - `type Runner = { id: string; nombre: string; activo: boolean }`
  - `type CuentaGestion = { id: string; nombre: string; moneda: Moneda; pago: number; activa: boolean }` (importar `Moneda` de `./tipos`)
  - `type Gestion = { id: string; fecha: string; runnerId: string; cuentaId: string; cuenta: string; tipo: TipoGestion; monto: number; pago: number; notas: string }`
  - `type TipoGestion = "retiro" | "transferencia"`
  - `type PagoRunner = { id: string; fecha: string; runnerId: string; monto: number; notas: string }`
  - `type SaldoRunner = { id: string; nombre: string; activo: boolean; gestionado: number; pagado: number; pendiente: number }`
  - `function calcularRunners(runners: Runner[], gestiones: Gestion[], pagos: PagoRunner[]): SaldoRunner[]`

- [ ] **Step 1: Escribir los tests**

```ts
import { describe, it, expect } from "vitest";
import { calcularRunners, type Runner, type Gestion, type PagoRunner } from "./runners";

const runner = (id: string, nombre: string, activo = true): Runner => ({ id, nombre, activo });
const gestion = (runnerId: string, pago: number): Gestion => ({
  id: crypto.randomUUID(), fecha: "2026-07-23", runnerId, cuentaId: "c1", cuenta: "Casa de cambio",
  tipo: "retiro", monto: 0, pago, notas: "",
});
const pago = (runnerId: string, monto: number): PagoRunner => ({
  id: crypto.randomUUID(), fecha: "2026-07-23", runnerId, monto, notas: "",
});

describe("calcularRunners", () => {
  it("suma el fee de las gestiones como gestionado", () => {
    const [s] = calcularRunners([runner("r1", "Owen")], [gestion("r1", 1000), gestion("r1", 500)], []);
    expect(s.gestionado).toBe(1500);
    expect(s.pagado).toBe(0);
    expect(s.pendiente).toBe(1500);
  });

  it("resta los pagos: pendiente = gestionado - pagado", () => {
    const [s] = calcularRunners([runner("r1", "Owen")], [gestion("r1", 1000)], [pago("r1", 400)]);
    expect(s.pendiente).toBe(600);
  });

  it("separa por runner", () => {
    const res = calcularRunners(
      [runner("r1", "Owen"), runner("r2", "Zurdo")],
      [gestion("r1", 1000), gestion("r2", 300)],
      [pago("r1", 1000)],
    );
    const owen = res.find((r) => r.id === "r1")!;
    const zurdo = res.find((r) => r.id === "r2")!;
    expect(owen.pendiente).toBe(0);
    expect(zurdo.pendiente).toBe(300);
  });

  it("un runner sin gestiones ni pagos queda en cero", () => {
    const [s] = calcularRunners([runner("r1", "Owen")], [], []);
    expect(s).toMatchObject({ gestionado: 0, pagado: 0, pendiente: 0 });
  });

  it("ignora gestiones/pagos de runners que no están en la lista", () => {
    const res = calcularRunners([runner("r1", "Owen")], [gestion("rX", 999)], [pago("rX", 999)]);
    expect(res).toHaveLength(1);
    expect(res[0].pendiente).toBe(0);
  });
});
```

- [ ] **Step 2: Correr y ver que fallan**

Run: `cd web && npx vitest run lib/cambio/runners.test.ts`
Expected: FAIL (módulo no existe).

- [ ] **Step 3: Implementar `runners.ts`**

Definir los tipos del bloque Interfaces y:

```ts
export function calcularRunners(runners: Runner[], gestiones: Gestion[], pagos: PagoRunner[]): SaldoRunner[] {
  return runners.map((r) => {
    const gestionado = gestiones.filter((g) => g.runnerId === r.id).reduce((a, g) => a + g.pago, 0);
    const pagado = pagos.filter((p) => p.runnerId === r.id).reduce((a, p) => a + p.monto, 0);
    return { id: r.id, nombre: r.nombre, activo: r.activo, gestionado, pagado, pendiente: gestionado - pagado };
  });
}
```

- [ ] **Step 4: Correr y ver que pasan**

Run: `cd web && npx vitest run lib/cambio/runners.test.ts` → PASS. Luego `npx tsc --noEmit` limpio.

- [ ] **Step 5: Commit**

```bash
git add web/lib/cambio/runners.ts web/lib/cambio/runners.test.ts
git commit -m "feat(cambio): tipos y cálculo puro de saldos de runners"
```

---

### Task 3: Capa de datos (lecturas)

**Files:**
- Create: `web/lib/cambio/runners-datos.ts`
- Test: `web/lib/cambio/runners-datos.test.ts`

**Interfaces:**
- Consumes: tipos de `./runners`, `createClient` de `@/lib/supabase/server`.
- Produces:
  - `getRunners(): Promise<Runner[]>` — de `runners`, ordenado por nombre.
  - `getCuentasGestion(): Promise<CuentaGestion[]>` — de `runner_accounts`, ordenado por nombre.
  - `getGestiones(): Promise<Gestion[]>` — de `runner_gestiones` con join `runner_accounts(name)` para `cuenta`, ordenado por `gestion_date` desc, `created_at` desc.
  - `getPagosRunner(): Promise<PagoRunner[]>` — de `runner_payments`, ordenado por `payment_date` desc.

- [ ] **Step 1: Escribir los tests** (mock del cliente Supabase igual que `datos.test.ts` si existe; si no, seguir el patrón de `actions.test.ts`). Cubrir: mapper snake→camel de cada lectura; que `getGestiones` mapea `runner_accounts.name`→`cuenta` (relación puede venir objeto o array de uno, usar helper `uno`); que un `kind`/`currency` desconocido cae a un default con aviso (reusar el patrón `unaDe` de `datos.ts`, o validación local). Correr y ver que fallan.

- [ ] **Step 2: Implementar** siguiendo `lib/cambio/datos.ts`: `LIMITE = 10000`, `.limit(LIMITE)`, `select("...", { count: "exact" })`, comparar `count` vs largo y `console.error` si trunca. Mappers a los tipos de `./runners`. Números vienen como `number | string` de PostgREST → `Number(...)`.

- [ ] **Step 3: Correr tests + tsc** → PASS/limpio.

- [ ] **Step 4: Commit**

```bash
git add web/lib/cambio/runners-datos.ts web/lib/cambio/runners-datos.test.ts
git commit -m "feat(cambio): lecturas de runners, cuentas, gestiones y pagos"
```

---

### Task 4: Server actions (altas)

**Files:**
- Create: `web/app/(app)/cambio/runners-actions.ts`
- Test: `web/app/(app)/cambio/runners-actions.test.ts`

**Interfaces:**
- Consumes: `createClient` de `@/lib/supabase/server`, `revalidatePath` de `next/cache`, `parsearMonto` de `@/lib/finanzas/montos`.
- Produces (todas `"use server"`, devuelven `ResultadoAlta`):
  - `createRunner(formData): Promise<ResultadoAlta>` — campo `name` (trim, no vacío).
  - `createRunnerAccount(formData): Promise<ResultadoAlta>` — `name`, `currency` (ARS|USD), `fee` (parsearMonto).
  - `createRunnerGestion(formData): Promise<ResultadoAlta>` — `gestionDate`, `runnerId`, `accountId`, `kind` (retiro|transferencia), `amount` (parsearMonto, default 0), `fee` (parsearMonto), `notes`.
  - `createRunnerPayment(formData): Promise<ResultadoAlta>` — `paymentDate`, `runnerId`, `amount` (parsearMonto), `notes`.

**Detalle clave:** cada action necesita el `company_id` de Gestiones MA. Seguir cómo lo resuelve `createExchangeOp` en `app/(app)/cambio/actions.ts` (leer esa fuente antes de implementar; probablemente hay un helper que obtiene la company del cambio). Reusar exactamente ese mecanismo.

- [ ] **Step 1: Leer `app/(app)/cambio/actions.ts`** para copiar el patrón de company_id, resultado discriminado, `revalidatePath` aislado y logging.

- [ ] **Step 2: Escribir los tests** (mock del cliente Supabase como en `actions.test.ts`): cada action inserta en la tabla correcta con los campos parseados; rechaza `name`/ids vacíos antes de tocar la base; `fee`/`amount` mal formados → error claro; `revalidatePath` que falla no tumba un insert exitoso (queda `{ok:true}`). Correr y ver que fallan.

- [ ] **Step 3: Implementar** las cuatro actions. Validar entradas antes de `createClient()`. `parsearMonto` devuelve `number | null` → si null en un campo obligatorio, `{ok:false,error:"..."}`.

- [ ] **Step 4: Correr tests + tsc + lint** → PASS/limpio.

- [ ] **Step 5: Commit**

```bash
git add web/app/(app)/cambio/runners-actions.ts web/app/(app)/cambio/runners-actions.test.ts
git commit -m "feat(cambio): server actions de alta de runners, cuentas, gestiones y pagos"
```

---

### Task 5: Componentes de formulario (modales)

**Files:**
- Create: `web/components/cambio/runner-forms.tsx`

**Interfaces:**
- Consumes: las actions de Task 4; tipos de `lib/cambio/runners`; `parsearMonto`, `formatearPesos`.
- Produces tres botones-con-modal client components:
  - `NuevaGestionButton({ runners, cuentas }: { runners: Runner[]; cuentas: CuentaGestion[] })` — modal con: fecha (`hoyISO`), runner (select), cuenta (select; `onChange` autocompleta el campo `fee` con `cuentas.find(...).pago`, editable), tipo (retiro/transferencia), monto movido (opcional), pago (editable), notas. Envía a `createRunnerGestion`.
  - `RegistrarPagoButton({ runners })` — modal: fecha, runner, monto, notas. Envía a `createRunnerPayment`.
  - `CuentasRunnerButton({ cuentas, runners }: ...)` — modal de config: lista de cuentas (nombre · moneda · `formatearPesos(pago)`), alta de cuenta (nombre, moneda, pago) vía `createRunnerAccount`, y alta de runner (nombre) vía `createRunner`.

**Patrón:** copiar la estructura de modal de `web/components/cambio/nueva-operacion-form.tsx` (overlay `className="cambio-modal-overlay"`, caja `className="cambio-modal"`, guard `guardando` contra doble submit, `cerrar()` que resetea estado, grids con `className="campo-fila"`, resultado discriminado del action con `if (r.ok) cerrar(); else setError(r.error)` y `catch` de red). Botón dorado `background: "var(--grad)"`.

- [ ] **Step 1: Implementar** los tres componentes en un archivo. Sin tests (componentes React; el proyecto no tiene jsdom). Verificar `npx tsc --noEmit` y `npm run lint` limpios, y `npx vitest run` sin regresiones.

- [ ] **Step 2: Commit**

```bash
git add web/components/cambio/runner-forms.tsx
git commit -m "feat(cambio): modales de gestión, pago y configuración de runners"
```

---

### Task 6: Historial (tarjetas/tabla) + página `/cambio/runners`

**Files:**
- Create: `web/components/cambio/runners-historial.tsx`
- Create: `web/app/(app)/cambio/runners/page.tsx`

**Interfaces:**
- Consumes: `getRunners`, `getCuentasGestion`, `getGestiones`, `getPagosRunner` (Task 3), `calcularRunners` (Task 2), los botones de Task 5, `MobileTopBar`, `formatearPesos`.
- `runners-historial.tsx` produce `RunnersHistorial({ gestiones, pagos, nombreRunner })` donde `nombreRunner: (id:string)=>string`. Muestra gestiones y pagos; mobile como `.ops-cards`, escritorio como `.ops-tabla` (mismo doble-render que `tabla-operaciones.tsx`).

- [ ] **Step 1: Implementar `runners-historial.tsx`** siguiendo el doble-render de `web/components/cambio/tabla-operaciones.tsx` (bloque `.ops-cards` + bloque `.ops-tabla`). Gestiones: fecha, runner, cuenta, tipo, `formatearPesos(pago)`. Pagos: fecha, runner, `formatearPesos(monto)`.

- [ ] **Step 2: Implementar la página** copiando la estructura de `web/app/(app)/cambio/page.tsx`: `export const dynamic = "force-dynamic"`, override `--accent`/`--grad` dorado, `<MobileTopBar />`, wrapper `className="cambio-page"`, encabezado `className="cambio-head"` con título "Runners", link de vuelta a `/cambio` (`prefetch` normal) y los botones `className="cambio-head-actions"` (NuevaGestión, RegistrarPago, CuentasRunner). Cargar datos con `Promise.all`, `calcularRunners`, y renderizar: tarjetas de saldo por runner (nombre, **pendiente** destacado en dorado, gestionado y pagado en gris) usando el mismo `panel` style, y abajo `<RunnersHistorial>`. Helper `nombreRunner` a partir de `getRunners`.

- [ ] **Step 3: Verificar** `npx tsc --noEmit`, `npm run lint`, `npx vitest run` (sin regresiones). Si hay dev server disponible, revisar `/cambio/runners`.

- [ ] **Step 4: Commit**

```bash
git add web/components/cambio/runners-historial.tsx "web/app/(app)/cambio/runners/page.tsx"
git commit -m "feat(cambio): página de runners con saldos e historial"
```

---

### Task 7: Enlace desde Cambio

**Files:**
- Modify: `web/app/(app)/cambio/page.tsx`

- [ ] **Step 1: Agregar** en el `cambio-head-actions` de la página de Cambio un `<Link href="/cambio/runners">` con el estilo secundario (igual que el botón "Descargar Excel": `background: "var(--card)", border: "1px solid var(--border)"`), texto **"Runners"**. Ubicarlo antes de "Descargar Excel".

- [ ] **Step 2: Verificar** `npx tsc --noEmit` y `npm run lint` limpios, `npx vitest run` sin regresiones.

- [ ] **Step 3: Commit**

```bash
git add "web/app/(app)/cambio/page.tsx"
git commit -m "feat(cambio): enlace a Runners desde la página de Cambio"
```

---

## Verificación final

- Correr `web/lib/db/sql/2026-07-23-runners.sql` en Supabase y confirmar: 3 runners (Owen, Zurdo, Capi) y las 4 tablas con RLS.
- Suite completa verde (`npx vitest run`), `tsc` y `lint` limpios, `npm run build` OK.
- Revisión final de rama antes de mergear a master y deployar (`npx vercel --prod`).
- Prueba en navegador (usuario): crear una cuenta de gestión con su pago, cargar una gestión (ver que autocompleta el pago), registrar un pago, y ver el saldo pendiente actualizado.
